---
phase: 03-options-auxiliary-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/css/pages.css
  - src/supporters.html
  - src/credits.html
  - src/updated.html
autonomous: true
requirements:
  - THM-01
  - AUX-01
must_haves:
  truths:
    - "supporters, credits, and updated pages render in both light and dark themes with no inline <style> block remaining"
    - "All three aux pages are linked to pages.css and have no hardcoded hex colors or inline style attributes"
    - "The Golden Key textarea (#textareaKeyCode) is monospace-framed by pages.css; its inline size attributes are removed"
    - "credits.html missing </html> closing tag is fixed"
    - "Playwright supporters.spec.js passes — #keyCodeValid and #keyCodeInvalid keep style='display: none'"
  artifacts:
    - path: "src/css/pages.css"
      provides: "Aux-page document layout, article container, pre/blockquote/code shells, golden key textarea, accent heading"
      min_lines: 60
      contains: "aesr-article"
    - path: "src/supporters.html"
      provides: "Inline <style> removed, pages.css linked, .aesr-article applied, .aesr-btn classes on sponsor button"
      contains: "css/pages.css"
    - path: "src/credits.html"
      provides: "Inline <style> removed, pages.css linked, .aesr-article applied, </html> closing tag added"
      contains: "css/pages.css"
    - path: "src/updated.html"
      provides: "Inline <style> removed, pages.css linked, .aesr-article applied, inline style attributes replaced with classes"
      contains: "css/pages.css"
  key_links:
    - from: "src/css/pages.css .aesr-article"
      to: "supporters.html, credits.html, updated.html body/wrapper"
      via: "class='aesr-article' applied to <body> or wrapper div"
      pattern: "aesr-article"
    - from: "src/css/pages.css #textareaKeyCode"
      to: "supporters.html #textareaKeyCode"
      via: "ID selector rule in pages.css replaces inline style='width: 62ex; height: 27em'"
      pattern: "id=\"textareaKeyCode\""
---

<objective>
Apply the Phase 1 token system to the three auxiliary pages: author `src/css/pages.css` and wire it into `src/supporters.html`, `src/credits.html`, and `src/updated.html` — removing their inline `<style>` blocks and replacing hardcoded inline style attributes with token-driven CSS classes.

Purpose: Completes AUX-01 and contributes to THM-01 ("every surface styled in both themes"). After this plan, no aux page has an inline `<style>` block, hardcoded palette, or `.darkMode` class. This plan runs in parallel with Plan 02 (no shared files).

Output: One new file (`src/css/pages.css`) and three modified HTML files. For each HTML file: CSS is authored AND the `<link>` is added AND the inline `<style>` is removed — atomically per surface (Pitfall 7: splitting these across commits leaves the page unstyled).
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-options-auxiliary-surfaces/03-RESEARCH.md
@.planning/phases/03-options-auxiliary-surfaces/03-PATTERNS.md
@.planning/phases/03-options-auxiliary-surfaces/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author src/css/pages.css</name>
  <files>src/css/pages.css</files>
  <read_first>
    src/css/popup.css — file-level header comment format (lines 1–5) and section banner format. Copy adapted for "Aux-page styles (Phase 3)".
    src/css/components.css — tab indentation style and token-only rule discipline to replicate.
    .planning/phases/03-options-auxiliary-surfaces/03-PATTERNS.md — §pages.css section for the complete list of shells to author and verbatim CSS specs (.aesr-article, .aesr-article section/p/li, .aesr-article code, .aesr-pre, .aesr-article blockquote, .aesr-article .aesr-heading-accent, #textareaKeyCode).
    .planning/phases/03-options-auxiliary-surfaces/03-UI-SPEC.md — §Component Inventory (Aux Pages Container, Golden Key Textarea, Blockquote, Accent heading, Pre block) for exact CSS property values.
    src/supporters.html — read to understand what inline styles exist on the key-code flex wrapper (line ~105) and status div (line ~107) that need moving to pages.css.
    src/updated.html — read to understand what the blockquote element currently looks like (border: 1px solid #666) and what inline styles remain on h1/h2/hr/spans.
  </read_first>
  <action>
    Create `src/css/pages.css` as a new file. File-level header comment: "pages.css — Aux-page styles for AESR (Phase 3: supporters, credits, updated). All classes use the .aesr-* prefix convention (locked in Phase 1). All color/size values use var(--token) — no hardcoded hex, no hardcoded px for dimensions that have a token. No @import, no !important, no color-mix(), no external font or icon URL."

    Section 1 — Article container (.aesr-article): `max-width: 720px; margin: 0 auto; padding: var(--space-m) var(--space-xxl); background-color: var(--color-bg-layout); color: var(--color-text-body); font-size: var(--font-size-body-m); line-height: 1.75;` (1.75 is an intentional exception for document reading rhythm — note in a comment). `.aesr-article section { margin-block: var(--space-xxl) 0; }`. `.aesr-article p, .aesr-article li { margin-block: var(--space-xxs) 0; }`.

    Section 2 — Scoped code (MUST be scoped under .aesr-article — not bare `code`): `.aesr-article code { font-family: var(--font-family-mono); color: var(--color-text-heading); font-weight: var(--font-weight-bold); }`.

    Section 3 — Pre block (.aesr-pre shared shell — also referenced from options.css): `.aesr-pre { box-sizing: border-box; background-color: var(--color-bg-input-disabled); border: 1px solid var(--color-border-divider); border-radius: var(--radius-input); font-family: var(--font-family-mono); font-size: var(--font-size-body-m); line-height: var(--line-height-body-m); padding: var(--space-xs); overflow-x: auto; white-space: pre; }`.

    Section 4 — Blockquote (scoped under .aesr-article): `.aesr-article blockquote { border: 1px solid var(--color-border-divider); border-radius: var(--radius-input); white-space: pre-line; width: 22em; padding: var(--space-xxs) var(--space-xs); margin: var(--space-m) var(--space-xxl); }`.

    Section 5 — Accent heading (scoped under .aesr-article): `.aesr-article .aesr-heading-accent { color: var(--color-text-accent); }`.

    Section 6 — Golden Key textarea (#textareaKeyCode): `font-family: var(--font-family-mono); font-size: var(--font-size-body-m); width: 62ex; height: 27em; border: 1px solid var(--color-border-input); border-radius: var(--radius-input); padding: var(--space-xs); background-color: var(--color-bg-input); color: var(--color-text-body); line-height: var(--line-height-body-m);`.

    Section 7 — Supporters page layout helpers: a flex wrapper class (e.g. `.aesr-key-row`) with `display: flex; align-items: flex-start; gap: var(--space-m);` (replaces the `style="display: flex"` on the key-code row wrapper); a status div margin rule for `.aesr-key-status { margin-left: var(--space-l); }` (replaces `style="margin-left: 20px"`); a small error label class `.aesr-label-error { color: var(--color-text-status-error); font-size: var(--font-size-body-s); }` (replaces `<span style="color:#a00; font-size: small;">`).

    Tab-indent all property declarations. No hex values. No bare `code` or `blockquote` rules (everything scoped under .aesr-article). Note the .aesr-pre class can also be referenced from options.css — it is shared via its class name.
  </action>
  <verify>
    <automated>grep -c '#[0-9a-fA-F]' src/css/pages.css</automated>
    Must return 0 (no hex). Also: `grep -c 'aesr-article\|aesr-pre\|aesr-heading-accent\|textareaKeyCode' src/css/pages.css` must return >= 4.
  </verify>
  <acceptance_criteria>
    - `src/css/pages.css` exists
    - `grep -E '#[0-9a-fA-F]{3}([0-9a-fA-F]{3})?' src/css/pages.css` returns 0 lines (no hex color values)
    - `grep -c 'aesr-article' src/css/pages.css` returns >= 1
    - `grep -c 'aesr-pre' src/css/pages.css` returns >= 1
    - `grep -c 'aesr-heading-accent' src/css/pages.css` returns >= 1
    - `grep -c 'textareaKeyCode' src/css/pages.css` returns >= 1
    - `grep -c 'aesr-article code' src/css/pages.css` returns >= 1 (code scoped, not bare)
    - `grep -c 'aesr-article blockquote' src/css/pages.css` returns >= 1
  </acceptance_criteria>
    <done>
    `src/css/pages.css` exists with all required shells: .aesr-article, .aesr-pre, .aesr-article blockquote, .aesr-article .aesr-heading-accent, #textareaKeyCode. Zero hex values. Tab-indented. All code/pre rules scoped under .aesr-article.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire pages.css into supporters.html, credits.html, and updated.html (atomic per surface)</name>
  <files>src/supporters.html, src/credits.html, src/updated.html</files>
  <read_first>
    src/supporters.html — read the full file. Key locations:
      - lines 1–9: head block (already has theme-init.js, tokens/base/components links)
      - lines 10–49: inline &lt;style&gt; block to remove (~40 lines including .sponsorButton, #textareaKeyCode hardcoded styles, background-color: #fafafa)
      - line ~51: body opening tag — add class="aesr-article"
      - line ~87: `<a class="sponsorButton">` — change to `class="aesr-btn aesr-btn--normal"`
      - line ~88: `<span style="color:#a00; font-size: small; ...">` — replace with `<span class="aesr-label-error">`
      - line ~105: key-code flex wrapper `<div style="display: flex">` — replace style= with `class="aesr-key-row"`
      - line ~106: `<textarea id="textareaKeyCode" style="width: 62ex; height: 27em">` — remove inline style= attributes (handled by pages.css #textareaKeyCode rule)
      - line ~107: status div `<div style="margin-left: 20px">` — replace with `class="aesr-key-status"`
      - KEEP verbatim: `#keyCodeValid style="display: none"` and `#keyCodeInvalid style="display: none"` — JS visibility contract; Playwright toBeHidden()/toBeVisible() depends on these
      - KEEP verbatim: `<script type="module" src="js/supporters.js">` after `</body>` — do NOT move to head

    src/credits.html — read the full file. Key locations:
      - lines 1–9: head block
      - lines 10–44: inline &lt;style&gt; block to remove (~35 lines including .pane hardcoded background-color: #eee)
      - line ~47: `<div class="pane" id="credits">` — change to `<div class="aesr-article" id="credits">` (or apply class="aesr-article" to body; executor's discretion — either approach produces the single-column layout)
      - line ~51: `<pre>` block (MIT license) — add `class="aesr-pre"`
      - end of file: add missing `</html>` closing tag

    src/updated.html — read the full file. Key locations:
      - lines 1–9: head block
      - lines 10–39: inline &lt;style&gt; block to remove (~30 lines including blockquote, code, section p hardcoded styles)
      - line ~41: body opening — add class="aesr-article"
      - line ~42: `<h1 style="margin:0">` — remove the `style="margin:0"` attribute (base.css already sets h1 { margin: 0 })
      - line ~45: `<h2 style="color:#0099f2">` — remove inline style, add `class="aesr-heading-accent"`
      - `<hr style="margin:18px 0">` — replace with `<hr class="aesr-divider">` (tokenized divider class from components.css)
      - `<h2>6.1.0 <span style="color:rgb(221, 63, 0)">New version!</span></h2>` — replace `style="color:..."` on the inner span with `class="aesr-heading-accent"` (uses --color-text-accent, closest token to the orange)
      - `<span style="margin-left:0.75em; color:rgb(221, 63, 0)">` (if present) — same treatment: replace with `class="aesr-heading-accent"` or remove inline style
      - The `<blockquote>` element: no class change needed — `.aesr-article blockquote` is a descendant selector in pages.css that applies automatically

    .planning/phases/03-options-auxiliary-surfaces/03-PATTERNS.md — §supporters.html, §credits.html, §updated.html sections for exact change tables.
  </read_first>
  <action>
    For EACH of the three HTML files — perform the CSS authoring + link + inline style removal atomically (all three in a single commit, or one commit per file — but each file's CSS + link + removal must not be split):

    src/supporters.html:
    - Add `<link rel="stylesheet" href="css/pages.css">` after `<link rel="stylesheet" href="css/components.css">` in the head
    - Remove the entire inline `<style>` block (lines 10–49)
    - Add `class="aesr-article"` to `<body>`
    - `<a class="sponsorButton">` → `class="aesr-btn aesr-btn--normal"`
    - `<span style="color:#a00; font-size: small; ...">` → `<span class="aesr-label-error">`
    - Key-code flex wrapper: `<div style="display: flex">` → `<div class="aesr-key-row">`
    - `<textarea id="textareaKeyCode" style="width: 62ex; height: 27em">` — remove inline style= (width and height now in pages.css via #textareaKeyCode selector)
    - Status div: `<div style="margin-left: 20px">` → `<div class="aesr-key-status">`
    - PRESERVE: `#keyCodeValid style="display: none"` unchanged
    - PRESERVE: `#keyCodeInvalid style="display: none"` unchanged
    - PRESERVE: `<script type="module" src="js/supporters.js">` after `</body>` placement

    src/credits.html:
    - Add `<link rel="stylesheet" href="css/pages.css">` after `<link rel="stylesheet" href="css/components.css">` in the head
    - Remove the entire inline `<style>` block (lines 10–44)
    - Apply `.aesr-article` to the `<div class="pane" id="credits">` (change class="pane" to class="aesr-article") or add class="aesr-article" to `<body>`
    - Add `class="aesr-pre"` to the `<pre>` block (MIT license)
    - Add `</html>` closing tag at end of file (currently missing)

    src/updated.html:
    - Add `<link rel="stylesheet" href="css/pages.css">` after `<link rel="stylesheet" href="css/components.css">` in the head
    - Remove the entire inline `<style>` block (lines 10–39)
    - Add `class="aesr-article"` to `<body>`
    - `<h1 style="margin:0">` — remove the style attribute
    - `<h2 style="color:#0099f2">` — remove style attribute, add `class="aesr-heading-accent"`
    - `<hr style="margin:18px 0">` → `<hr class="aesr-divider">`
    - `<span style="color:rgb(221, 63, 0)">` on version heading spans — replace inline style with `class="aesr-heading-accent"`
    - No class change needed on `<blockquote>` — `.aesr-article blockquote` descendant selector handles it
  </action>
  <verify>
    <automated>npm run test_emulator</automated>
    Must pass including supporters.spec.js (which verifies #keyCodeValid and #keyCodeInvalid display states).

    Additional source assertions:
    - `grep -c '<style>' src/supporters.html` → 0
    - `grep -c '<style>' src/credits.html` → 0
    - `grep -c '<style>' src/updated.html` → 0
    - `grep -c 'css/pages.css' src/supporters.html` → 1
    - `grep -c 'css/pages.css' src/credits.html` → 1
    - `grep -c 'css/pages.css' src/updated.html` → 1
    - `grep -c 'style="display: none"' src/supporters.html` → 2 (keyCodeValid and keyCodeInvalid — MUST remain)
    - `grep -c '#a00\|color:rgb\|color:#0099' src/supporters.html src/credits.html src/updated.html` → 0 (no remaining hardcoded colors)
    - `grep -c '</html>' src/credits.html` → 1 (closing tag added)
    - `npm test` → passes
  </verify>
  <acceptance_criteria>
    - `grep -c '<style>' src/supporters.html` returns 0
    - `grep -c '<style>' src/credits.html` returns 0
    - `grep -c '<style>' src/updated.html` returns 0
    - `grep -c 'css/pages.css' src/supporters.html` returns 1
    - `grep -c 'css/pages.css' src/credits.html` returns 1
    - `grep -c 'css/pages.css' src/updated.html` returns 1
    - `grep -c 'style="display: none"' src/supporters.html` returns 2 (#keyCodeValid and #keyCodeInvalid preserved)
    - `grep -c '</html>' src/credits.html` returns 1 (closing tag added)
    - `grep -E "color:#[0-9a-fA-F]|color:rgb" src/supporters.html src/credits.html src/updated.html` returns 0 (no hardcoded inline colors)
    - `npm test` exits 0
    - `npm run test_emulator` exits 0 (supporters.spec.js passes including toBeHidden/toBeVisible on #keyCodeInvalid)
  </acceptance_criteria>
    <done>
    All three aux pages have: inline `<style>` block removed, `pages.css` linked after `components.css`, `.aesr-article` applied to body or wrapper, no hardcoded hex or rgb inline styles remaining.
    `src/supporters.html`: `#keyCodeValid` and `#keyCodeInvalid` retain `style="display: none"`, `<script>` placement after `</body>` preserved, `#textareaKeyCode` inline size attributes removed.
    `src/credits.html`: `</html>` closing tag present.
    `src/updated.html`: `<h2 style="color:#0099f2">` replaced with `class="aesr-heading-accent"`, `<hr>` uses `class="aesr-divider"`, version span inline colors replaced.
    `npm test` and `npm run test_emulator` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML/CSS | All changes are static HTML restructuring and CSS authoring; no new JS paths |
| supporters.js visibility contract | #keyCodeValid / #keyCodeInvalid display toggled by supporters.js; Phase 3 must not interfere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (CSP violation) | Aux page inline style removal | mitigate | All styling moved to static `pages.css` file; no inline `<style>` blocks remain; no new inline style attributes introduced except `style="display: none"` which is pre-existing and JS-managed |
| T-03-06 | Tampering (Playwright regression) | supporters.html #keyCodeValid / #keyCodeInvalid | mitigate | `style="display: none"` attributes on both elements PRESERVED verbatim; `supporters.js` manages these; Phase 3 does not touch `supporters.js`; verified by `grep -c 'style="display: none"' src/supporters.html` → 2 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete:

1. `npm test` passes (0 failures)
2. `npm run test_emulator` passes (supporters.spec.js: toBeHidden()/toBeVisible() on #keyCodeInvalid/#keyCodeValid passes)
3. `grep -c '<style>' src/supporters.html src/credits.html src/updated.html` → 0 for each
4. `grep -c 'css/pages.css' src/supporters.html src/credits.html src/updated.html` → 1 for each
5. `grep -c 'style="display: none"' src/supporters.html` → 2 (keyCodeValid + keyCodeInvalid preserved)
6. `grep -c '</html>' src/credits.html` → 1
7. `grep -c '#[0-9a-fA-F]' src/css/pages.css` → 0
</verification>

<success_criteria>
- `src/css/pages.css` exists with all required shells, zero hex, tab-indented, token-only values
- All three aux pages have `<style>` blocks removed, `pages.css` linked, `.aesr-article` applied, no hardcoded inline style attributes (except `style="display: none"` on #keyCodeValid/#keyCodeInvalid in supporters.html which is preserved)
- `src/credits.html` has `</html>` closing tag
- `npm test` and `npm run test_emulator` pass with 0 failures
</success_criteria>

<output>
Create `.planning/phases/03-options-auxiliary-surfaces/03-03-SUMMARY.md` when done.
</output>
