---
phase: 03-options-auxiliary-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/css/tokens.css
  - src/css/components.css
autonomous: true
requirements:
  - THM-01
  - OPT-04
must_haves:
  truths:
    - "Primary button hover color renders correctly in dark mode (no navy-on-dark fallthrough)"
    - "Alert success and warning variants are available as shared component classes"
    - "All dark-theme token layers define --color-bg-button-primary-hover"
  artifacts:
    - path: "src/css/tokens.css"
      provides: "--color-bg-button-primary-hover in both Layer 2 and Layer 3a dark blocks"
      contains: "--color-bg-button-primary-hover: #1a73e8"
    - path: "src/css/components.css"
      provides: ".aesr-alert--success and .aesr-alert--warning modifier classes"
      contains: "aesr-alert--success"
  key_links:
    - from: "src/css/tokens.css"
      to: "src/css/options.css (Plan 02)"
      via: "var(--color-bg-button-primary-hover) consumed by .aesr-btn--primary:hover"
      pattern: "color-bg-button-primary-hover"
    - from: "src/css/components.css"
      to: "src/js/options.js updateMessage()"
      via: ".aesr-alert--success / .aesr-alert--warning injected by updateMessage()"
      pattern: "aesr-alert--success|aesr-alert--warning"
---

<objective>
This plan closes two blocking prerequisites before any options-page or aux-page CSS can ship:
(1) A token gap: `--color-bg-button-primary-hover` is defined only in Layer 1 (light) of `tokens.css` — dark Layers 2 and 3a fall through to the light navy value `#004a9e`, which renders incorrectly against the dark `#42b4ff` primary button background.
(2) Missing shared alert modifier classes in `components.css`: `updateMessage()` in `options.js` will inject `.aesr-alert--success` and `.aesr-alert--warning`, but those modifier classes do not yet exist.

Purpose: Plan 02 (options.css + options.html + options.js) and Plan 03 (pages.css + aux HTML) both depend on these tokens and components. Wave 1 unblocks Wave 2.

Output: Two file edits — one insertion per dark block in `tokens.css`, and two new modifier blocks appended to `components.css`.
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-options-auxiliary-surfaces/03-RESEARCH.md
@.planning/phases/03-options-auxiliary-surfaces/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-bg-button-primary-hover to dark Layers 2 and 3a in tokens.css</name>
  <files>src/css/tokens.css</files>
  <read_first>
    src/css/tokens.css — read the full file to locate: (a) Layer 1 line ~30 where `--color-bg-button-primary: #0062b5` and `--color-bg-button-primary-hover: #004a9e` appear together; (b) Layer 2 block starting with `@media (prefers-color-scheme: dark)` around line 82 where `--color-bg-button-primary: #42b4ff` appears but `--color-bg-button-primary-hover` is absent; (c) Layer 3a block starting with `:root[data-theme="dark"]` around line 121 where `--color-bg-button-primary: #42b4ff` appears but `--color-bg-button-primary-hover` is absent.
  </read_first>
  <action>
    In `tokens.css`, add `--color-bg-button-primary-hover: #1a73e8;` in two locations:

    Layer 2 (OS dark media query block, `@media (prefers-color-scheme: dark)` → `:root:not([data-theme="light"]):not([data-theme="dark"])`): Insert immediately after the line that declares `--color-bg-button-primary: #42b4ff;`. The insertion must be on a new line, indented with tabs to match the surrounding declarations, in the same position relative to `--color-bg-button-primary` as in Layer 1.

    Layer 3a (explicit dark override block, `:root[data-theme="dark"]`): Insert immediately after the line that declares `--color-bg-button-primary: #42b4ff;`. Same tab indentation. Same relative position.

    The value `#1a73e8` is blue-700 from the Cloudscape dark palette (Phase 1 STACK.md reference). Do not change any other token. Do not modify Layer 1.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*/\*' src/css/tokens.css | grep -c '\-\-color-bg-button-primary-hover'</automated>
    Expected output: 3 (one in Layer 1, one in Layer 2, one in Layer 3a). Run: `grep -n 'color-bg-button-primary-hover' src/css/tokens.css` to confirm line numbers and values.
  </verify>
  <done>
    `src/css/tokens.css` contains exactly 3 declarations of `--color-bg-button-primary-hover`: the existing light value `#004a9e` in Layer 1, and `#1a73e8` in both Layer 2 and Layer 3a. The value appears immediately after `--color-bg-button-primary: #42b4ff;` in each dark block. No other tokens changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .aesr-alert--success and .aesr-alert--warning modifier classes to components.css</name>
  <files>src/css/components.css</files>
  <read_first>
    src/css/components.css — read lines 241–270 (end of the `.aesr-alert` base block, `.aesr-alert--error`, `.aesr-alert__heading`, `.aesr-alert__body`). The insertion point is immediately after the closing `}` of the `.aesr-alert__body` block (currently the last alert-related rule). Verify the exact tab indentation style in use. Confirm `.aesr-alert--success` and `.aesr-alert--warning` are absent by grepping.
    .planning/phases/03-options-auxiliary-surfaces/03-RESEARCH.md — §Component Inventory for the exact modifier class bodies and specificity rationale.
    .planning/phases/03-options-auxiliary-surfaces/03-PATTERNS.md — §components.css section for the verbatim CSS to insert and its insertion point.
  </read_first>
  <action>
    Append the following four rule blocks to `components.css`, immediately after the closing `}` of the `.aesr-alert__body` block (end of the existing alert section), before any next section separator:

    `.aesr-alert--success` with `border-left: 4px solid var(--color-text-status-success)` and `background-color: var(--color-bg-container)`.

    `.aesr-alert--success .aesr-alert__body` with `color: var(--color-text-status-success)`. This specificity (0,2,0) beats `.aesr-alert__body` (0,1,0), overriding the default error color.

    `.aesr-alert--warning` with `border-left: 4px solid var(--color-text-status-warning)` and `background-color: var(--color-bg-container)`.

    `.aesr-alert--warning .aesr-alert__body` with `color: var(--color-text-status-warning)`.

    Use tab indentation for all property declarations, matching the existing components.css style. Use `var(--token)` only — no hex values. Add a blank line between each block, matching the existing pattern between `.aesr-alert--error` and `.aesr-alert__heading`.
  </action>
  <verify>
    <automated>grep -c 'aesr-alert--success\|aesr-alert--warning' src/css/components.css</automated>
    Expected output: 4 (two class selectors for success, two for warning). Also run: `npm test` — must pass with 0 failures.
  </verify>
  <done>
    `src/css/components.css` contains `.aesr-alert--success`, `.aesr-alert--success .aesr-alert__body`, `.aesr-alert--warning`, and `.aesr-alert--warning .aesr-alert__body` modifier rules. All values are `var(--token)`. Tab-indented. `npm test` passes. No existing rules modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static CSS | Changes are static; no runtime eval path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | tokens.css dark layer additions | mitigate | Values are hardcoded hex literals from the Cloudscape palette; no user input, no dynamic generation; verified by grep count |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan — N/A; package legitimacy audit in 03-RESEARCH.md §Package Legitimacy Audit confirms zero new dependencies |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c '\-\-color-bg-button-primary-hover' src/css/tokens.css` → 3
2. `grep -n 'color-bg-button-primary-hover' src/css/tokens.css` → line in Layer 1 (value `#004a9e`) + line in Layer 2 (value `#1a73e8`) + line in Layer 3a (value `#1a73e8`)
3. `grep -c 'aesr-alert--success\|aesr-alert--warning' src/css/components.css` → 4
4. `npm test` → all tests pass (no CSS changes affect JS unit tests; verifies no accidental file corruption)
</verification>

<success_criteria>
- `--color-bg-button-primary-hover: #1a73e8` appears in both dark layers (Layer 2 and Layer 3a) of `tokens.css`, immediately after `--color-bg-button-primary: #42b4ff` in each
- `.aesr-alert--success`, `.aesr-alert--success .aesr-alert__body`, `.aesr-alert--warning`, and `.aesr-alert--warning .aesr-alert__body` exist in `components.css`
- All values are `var(--token)` — no hex in `components.css` additions
- `npm test` passes with 0 failures
</success_criteria>

<notes>
## Wave 0 Gate

This plan is Wave 1 and must complete before Plans 02 and 03. Plan 02 depends on the dark hover token to prevent primary button hover regression. Plan 02 also depends on the alert modifier classes to wire `updateMessage()`.
</notes>

<output>
Create `.planning/phases/03-options-auxiliary-surfaces/03-01-SUMMARY.md` when done.
</output>
