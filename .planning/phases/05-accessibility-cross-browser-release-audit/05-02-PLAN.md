---
phase: 05-accessibility-cross-browser-release-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - test/emulator/a11y.spec.js
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - A11Y-01
  - A11Y-04
  - A11Y-05
user_setup: []

must_haves:
  truths:
    - "The full unit + emulator suite is green after the Plan 01 code changes land"
    - "A11Y verification coverage is on record (either axe spec or acknowledged manual-only)"
    - "Firefox and Edge have been manually smoked in both themes"
  artifacts:
    - path: "test/emulator/a11y.spec.js"
      provides: "Axe-core WCAG scans on popup + options in both themes (if option-yes chosen in Plan 01)"
  key_links:
    - from: "test/emulator/a11y.spec.js"
      to: "fixtures.js testInPopup / testInOptions"
      via: "import { testInPopup, testInOptions } from './fixtures.js'"
      pattern: "testInPopup|testInOptions"
---

<objective>
Run the full test suite and execute a11y verification — either automated via axe-core spec (if option-yes was chosen) or confirmed via manual smoke. Then execute the cross-browser manual smoke for Firefox and Edge (A11Y-05).

Purpose: Validates that all Plan 01 code changes landed cleanly (no broken selectors, no test regressions) and that A11Y-01 (focus visibility) and A11Y-02 (label coverage) are confirmed via automated scan or acknowledged manual check.

Output: Green npm test + npm run test_emulator; test/emulator/a11y.spec.js (if axe path chosen); manual smoke confirmed for Chrome, Firefox, Edge in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-RESEARCH.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite; add axe spec if option-yes (A11Y-01, A11Y-04)</name>
  <files>test/emulator/a11y.spec.js, package.json, package-lock.json</files>
  <read_first>
    .planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md — confirm option-yes or option-no decision from Plan 01 checkpoint
    test/emulator/fixtures.js — confirm testInPopup, testInOptions, testInSupporters signatures before writing a11y.spec.js
    test/emulator/visual_mode.spec.js — reference for testInOptions pattern (before/pageFunc/after structure)
    test/emulator/supporters.spec.js — reference for testInSupporters pattern (simple two-arg form)
    .planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md — section "test/emulator/a11y.spec.js" for axe call sequence and dual-theme scan pattern
  </read_first>
  <action>
    First: run npm test to confirm unit tests still pass after Plan 01 changes. If any test fails, debug before proceeding.

    If option-no was chosen in Plan 01 checkpoint:
    - Run npm test && npm run test_emulator to confirm the full suite is green
    - No new files to create — document test results in the summary
    - This task is complete when both commands exit 0

    If option-yes was chosen in Plan 01 checkpoint:
    FIRST — blocking legitimacy checkpoint before install (see Task checkpoint below — this task runs AFTER that checkpoint clears).

    After legitimacy confirmed: install the devDep:
    npm install --save-dev @axe-core/playwright
    This updates package.json devDependencies and package-lock.json.

    Create test/emulator/a11y.spec.js with the following structure:
    - Import AxeBuilder from '@axe-core/playwright'
    - Import testInPopup and testInOptions from './fixtures.js'
    - Test 1: "a11y: popup passes WCAG 2.1 AA in light theme"
      Use testInPopup with before=chrome.storage.sync.clear, pageFunc that runs AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze() and asserts violations equals []
    - Test 2: "a11y: popup passes WCAG 2.1 AA in dark theme"
      Use testInPopup with before setting visualMode:'dark' in chrome.storage.sync, pageFunc that sets data-theme via page.evaluate and runs axe scan, after clearing storage
    - Test 3: "a11y: options page passes WCAG 2.1 AA in light theme"
      Use testInOptions with before=clear, pageFunc runs axe scan, after=clear
    - Test 4: "a11y: options page passes WCAG 2.1 AA in dark theme"
      Use testInOptions with before setting visualMode:'dark', pageFunc sets dark theme and runs axe scan, after=clear

    For the dark-theme pageFunc pattern: use page.evaluate(() => chrome.storage.sync.set({ visualMode: 'dark' })) then page.waitForTimeout(300) then verify document.documentElement.getAttribute('data-theme') === 'dark' before running axe (mirrors visual_mode.spec.js lines 44-50).

    After creating a11y.spec.js, run npm run test_emulator to confirm all specs pass including the new a11y spec.
  </action>
  <verify>
    <automated>npm test && npm run test_emulator</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (all unit tests pass — confirms Plan 01 JS/test changes landed correctly)
    - npm run test_emulator exits 0 (all emulator specs pass — confirms HTML aria-label additions did not break any ID-selector-based Playwright locator)
    - If option-yes: test/emulator/a11y.spec.js exists and contains 4 test cases covering popup light, popup dark, options light, options dark
    - If option-yes: a11y.spec.js imports AxeBuilder from '@axe-core/playwright' and uses .withTags(['wcag2a', 'wcag2aa'])
    - If option-yes: package.json devDependencies contains @axe-core/playwright
    - If option-no: both test commands exit 0 and no new files are created
  </acceptance_criteria>
  <done>Full test suite green; a11y coverage approach implemented per user decision</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 applied all code fixes (ARIA labels, contrast tokens, swatch color). Plan 02 Task 1 ran the full test suite (and optionally created the axe spec).
  </what-built>
  <how-to-verify>
    Manually smoke test the extension in Firefox and Edge in both themes (A11Y-05). The Playwright emulator runs Chrome only — Firefox and Edge require your installed browsers.

    Steps to load the extension:
    1. Run: npm run build (if not already done — confirms dist/ is fresh)
    2. Firefox: about:debugging → "This Firefox" → Load Temporary Add-on → select dist/firefox/manifest.json
    3. Edge: edge://extensions → Developer mode → Load unpacked → select dist/chrome/

    Smoke checklist (repeat for each browser, both light and dark themes):

    POPUP (click extension icon or visit chrome/edge-extension:// URL):
    - [ ] Role list renders — items visible with correct font and spacing
    - [ ] Color swatches render — default gray swatch color visible against item background
    - [ ] Filter input accessible — focus visible with blue outline ring
    - [ ] Arrow-key navigation works — focus moves between role items
    - [ ] Light/dark toggle applied — switch theme via Options; popup reflects the change

    OPTIONS PAGE (Options link or chrome/edge-extension options):
    - [ ] Page layout renders — containers, cards, header correct
    - [ ] INI textarea accessible — focus visible, content editable
    - [ ] Color picker pair accessible — picker and hex input both focusable with visible ring
    - [ ] Visual mode radios work — selecting each changes page theme
    - [ ] Form controls (radios, checkboxes) styled correctly in both themes

    NATIVE CONTROLS (critical for Firefox/Edge cross-browser parity):
    - [ ] Scrollbars follow theme (dark scrollbars in dark mode) — thanks to color-scheme token
    - [ ] Checkboxes render in theme-appropriate style
    - [ ] Textarea border visible in dark theme — swatch border uses updated #6e6e7a

    AUXILIARY PAGES (if accessible via extension):
    - [ ] Supporters page renders correctly; key code textarea focusable
  </how-to-verify>
  <resume-signal>Type "smoke-passed" if all browsers pass both themes, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install (if axe-core) → devDependency | Package install touches only node_modules and lockfile; does not affect extension bundle |
| Playwright test → chrome-extension:// URL | Extension runs in isolated browser context; axe injects via page.evaluate — same trust boundary as existing emulator tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | @axe-core/playwright npm install | mitigate | Package is [ASSUMED] — legitimacy checkpoint before install (verify @axe-core/playwright v4.11.3 on npmjs.com/package/@axe-core/playwright: confirm Deque authorship, no postinstall script, correct source repo github.com/dequelabs/axe-core-npm) |
| T-05-02-02 | Information Disclosure | axe scan output in test logs | accept | axe reports HTML structure and ARIA attributes — no PII, no secrets; test output is local dev only |
| T-05-SC | Tampering | npm installs generally | mitigate | Legitimacy checkpoint is blocking-human; applies to @axe-core/playwright if option-yes |
</threat_model>

<verification>
After Task 1:
- npm test exits 0
- npm run test_emulator exits 0

After manual smoke checkpoint:
- Firefox popup + options: both themes render correctly
- Edge popup + options: both themes render correctly
- Native controls follow theme in both browsers
</verification>

<success_criteria>
- npm test + npm run test_emulator green (zero regressions from Plan 01 changes)
- A11Y coverage documented — axe spec (option-yes) or acknowledged manual-only (option-no)
- Firefox and Edge manually smoked in both themes; smoke-passed confirmed
</success_criteria>

<output>
Create `.planning/phases/05-accessibility-cross-browser-release-audit/05-02-SUMMARY.md` when done
</output>
