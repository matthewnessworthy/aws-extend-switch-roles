---
phase: 05-accessibility-cross-browser-release-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup.html
  - src/options.html
  - src/supporters.html
  - src/css/tokens.css
  - src/js/lib/create_role_list_item.js
  - src/js/lib/create_role_list_item.test.js
autonomous: false
requirements:
  - A11Y-02
  - A11Y-03
user_setup: []

must_haves:
  truths:
    - "Every interactive element gap has an explicit accessible name via aria-label or alt"
    - "The dark swatch border contrast ratio meets WCAG 1.4.11 3:1 in both dark cascade paths"
    - "The default swatch fill renders at 4.54:1 in light and 3.49:1 in dark"
    - "The unit test suite passes without modification to the dataset.color assertion"
  artifacts:
    - path: "src/popup.html"
      provides: "aria-label on #roleFilter; aria-label on #roleList; alt on #goldenkey"
      contains: 'aria-label="Filter roles"'
    - path: "src/options.html"
      provides: "aria-label on #awsConfigTextArea; aria-label on #colorPicker; aria-label on #colorValue; aria-hidden on decorative <b>"
      contains: 'aria-label="AWS config (INI format)"'
    - path: "src/supporters.html"
      provides: "aria-label on #textareaKeyCode"
      contains: 'aria-label="Supporter key code"'
    - path: "src/css/tokens.css"
      provides: "Updated --color-border-input in both dark cascade layers"
      contains: "#6e6e7a"
    - path: "src/js/lib/create_role_list_item.js"
      provides: "Default swatch fill #767676 replacing #aaaaaa"
      contains: "'#767676'"
    - path: "src/js/lib/create_role_list_item.test.js"
      provides: "Updated innerHTML assertions for new fill color"
      contains: "rgb(118, 118, 118)"
  key_links:
    - from: "src/js/lib/create_role_list_item.js"
      to: "src/js/lib/create_role_list_item.test.js"
      via: "inline style background-color assertion"
      pattern: "rgb\\(118, 118, 118\\)"
    - from: "src/css/tokens.css"
      to: "swatch border rendering"
      via: "--color-border-input in two dark blocks"
      pattern: "--color-border-input: #6e6e7a"
---

<objective>
Fix all identified ARIA label gaps and WCAG contrast failures across source HTML, CSS, and JS files — the concrete code changes that make A11Y-02 and A11Y-03 pass.

Purpose: The design system established a11y infrastructure (focus rings, token contrast). Seven specific gaps remain (missing labels/alt) and two contrast values fail WCAG 1.4.11. All must be fixed before smoke testing and release.

Output: Updated src/popup.html, src/options.html, src/supporters.html, src/css/tokens.css, src/js/lib/create_role_list_item.js, src/js/lib/create_role_list_item.test.js. Unit test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-RESEARCH.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ARIA labels and alt to HTML gaps (A11Y-02)</name>
  <files>src/popup.html, src/options.html, src/supporters.html</files>
  <read_first>
    src/popup.html — read in full before touching; find lines for #roleFilter (line ~17), #roleList (line ~19), #goldenkey (line ~42)
    src/options.html — read in full before touching; find lines for #awsConfigTextArea (line ~25), the .aesr-color-pair block with #colorPicker and #colorValue (lines ~27-29)
    src/supporters.html — read in full before touching; find line for #textareaKeyCode (line ~67); confirm #keyCodeValid and #keyCodeInvalid retain style="display: none"
    .planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md — Sections "src/popup.html", "src/options.html", "src/supporters.html" for exact attribute values and constraints
  </read_first>
  <behavior>
    - Gap 1: #roleFilter gains aria-label="Filter roles" (attribute added inline, no structural change)
    - Gap 2: #roleList gains aria-label="AWS roles" (no role attribute added — ul/li/a implicit semantics are correct)
    - Gap 3: #goldenkey img gains alt="Thank you for your support." (title attribute remains unchanged; style="display: none" unchanged)
    - Gap 4: #awsConfigTextArea gains aria-label="AWS config (INI format)" (no visible label added — aria-label sufficient)
    - Gap 5: #colorPicker gains aria-label="Profile color"; #colorValue gains aria-label="Profile color hex value"; the decorative &lt;b&gt;#&lt;/b&gt; between them gains aria-hidden="true". DO NOT add class="sr-only" — that class does not exist in src/css/ and would render as a visible label
    - Gap 6: #textareaKeyCode in supporters.html gains aria-label="Supporter key code"
    - MUST NOT change: style="display: none" on #keyCodeValid and #keyCodeInvalid in supporters.html (Playwright assertions depend on them)
    - MUST NOT change: any existing id, class, or name attribute on any of the above elements
  </behavior>
  <action>
    Edit src/popup.html:
    - On the input#roleFilter element add attribute: aria-label="Filter roles" (after the existing type="text" attribute)
    - On the ul#roleList element add attribute: aria-label="AWS roles"
    - On the img#goldenkey element add attribute: alt="Thank you for your support." (insert before the existing title attribute)

    Edit src/options.html:
    - On the textarea#awsConfigTextArea element add attribute: aria-label="AWS config (INI format)"
    - On the input#colorPicker element add attribute: aria-label="Profile color"
    - On the b element containing the literal "#" between colorPicker and colorValue add attribute: aria-hidden="true"
    - On the input#colorValue element add attribute: aria-label="Profile color hex value"

    Edit src/supporters.html:
    - On the textarea#textareaKeyCode element add attribute: aria-label="Supporter key code"

    Verify after each file save that no element was moved or restructured — only attributes were added.
  </action>
  <verify>
    <automated>grep -c 'aria-label="Filter roles"' src/popup.html && grep -c 'aria-label="AWS roles"' src/popup.html && grep -c 'alt="Thank you for your support."' src/popup.html && grep -c 'aria-label="AWS config (INI format)"' src/options.html && grep -c 'aria-label="Profile color"' src/options.html && grep -c 'aria-label="Profile color hex value"' src/options.html && grep -c 'aria-label="Supporter key code"' src/supporters.html</automated>
  </verify>
  <acceptance_criteria>
    - src/popup.html contains aria-label="Filter roles" on the input#roleFilter element (grep returns 1)
    - src/popup.html contains aria-label="AWS roles" on ul#roleList (grep returns 1)
    - src/popup.html contains alt="Thank you for your support." on img#goldenkey (grep returns 1); title attribute still present on same element
    - src/options.html contains aria-label="AWS config (INI format)" on textarea#awsConfigTextArea (grep returns 1)
    - src/options.html contains aria-label="Profile color" on input#colorPicker (grep returns 1)
    - src/options.html contains aria-label="Profile color hex value" on input#colorValue (grep returns 1)
    - src/options.html contains aria-hidden="true" on the b element containing "#"
    - src/supporters.html contains aria-label="Supporter key code" on textarea#textareaKeyCode (grep returns 1)
    - src/supporters.html still contains style="display: none" on both #keyCodeValid and #keyCodeInvalid
    - No element id, class, or structural nesting was changed in any of the three files
  </acceptance_criteria>
  <done>All 6 ARIA gaps patched with attribute-only additions; unit test suite passes (npm test, no HTML changes affect unit tests)</done>
</task>

<task type="auto">
  <name>Task 2: Fix contrast failures — tokens.css and swatch default (A11Y-03)</name>
  <files>src/css/tokens.css, src/js/lib/create_role_list_item.js, src/js/lib/create_role_list_item.test.js</files>
  <read_first>
    src/css/tokens.css — read lines 100-160; confirm line 109 is inside the @media (prefers-color-scheme: dark) block and line 148 is inside the :root[data-theme="dark"] block; confirm both show --color-border-input: #656871
    src/js/lib/create_role_list_item.js — read in full; confirm line 10 has headSquare.style.backgroundColor = '#aaaaaa' and line 35 has anchor.dataset.color = item.color || 'aaaaaa'
    src/js/lib/create_role_list_item.test.js — read lines 35-50 and lines 160-170; confirm rgb(170, 170, 170) at lines 41 and 165; confirm dataset.color assertion ('aaaaaa') at line 38
    .planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md — sections "src/css/tokens.css" and "src/js/lib/create_role_list_item.js" for exact values and constraints
  </read_first>
  <action>
    Edit src/css/tokens.css:
    - Change --color-border-input: #656871 at line 109 (inside the @media (prefers-color-scheme: dark) :root:not([data-theme="light"]):not([data-theme="dark"]) block) to --color-border-input: #6e6e7a
    - Change --color-border-input: #656871 at line 148 (inside the :root[data-theme="dark"] block) to --color-border-input: #6e6e7a
    - Both occurrences must be updated — missing either means the fix applies to only one dark activation path (OS-auto vs explicit)

    Edit src/js/lib/create_role_list_item.js:
    - Change headSquare.style.backgroundColor = '#aaaaaa' (line ~10) to headSquare.style.backgroundColor = '#767676'
    - DO NOT change anchor.dataset.color = item.color || 'aaaaaa' on line ~35 — that is the stored profile key default for tab group dispatch, not the CSS fill, and changing it would be a behavioral regression

    Edit src/js/lib/create_role_list_item.test.js:
    - Change the innerHTML assertion at line 41 from background-color: rgb(170, 170, 170) to background-color: rgb(118, 118, 118)
    - Change the innerHTML assertion at line 165 from background-color: rgb(170, 170, 170) to background-color: rgb(118, 118, 118)
    - DO NOT change expect(a.dataset.color).to.eq('aaaaaa') at line 38 — this tests the stored config default key value, not the CSS fill
  </action>
  <verify>
    <automated>grep -v '#' src/css/tokens.css | grep -c '656871' ; echo "expect: 0" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/css/tokens.css contains zero occurrences of #656871 (both dark cascade layers updated to #6e6e7a)
    - src/css/tokens.css contains exactly 2 occurrences of #6e6e7a (one in the @media block, one in the :root[data-theme="dark"] block): grep -c '6e6e7a' src/css/tokens.css returns 2
    - src/js/lib/create_role_list_item.js line ~10 contains '#767676' (not '#aaaaaa')
    - src/js/lib/create_role_list_item.js line ~35 still contains 'aaaaaa' in the dataset.color fallback
    - src/js/lib/create_role_list_item.test.js contains zero occurrences of rgb(170, 170, 170): grep -c 'rgb(170, 170, 170)' returns 0
    - src/js/lib/create_role_list_item.test.js contains 2 occurrences of rgb(118, 118, 118): grep -c 'rgb(118, 118, 118)' returns 2
    - src/js/lib/create_role_list_item.test.js still contains expect(a.dataset.color).to.eq('aaaaaa') (line ~38)
    - npm test exits 0 (all 39 unit tests pass)
  </acceptance_criteria>
  <done>Dark border contrast passes 3:1 (2.85 → 3.15) in both dark activation paths; default swatch fill passes 4.5:1 in light; unit tests green</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Add @axe-core/playwright as a devDependency for automated WCAG verification in Plan 02?</decision>
  <context>
    Plan 02 needs to verify A11Y-01 (focus indicators) and A11Y-02 (label coverage) programmatically.
    Without axe-core: Plan 02 relies on npm test + npm run test_emulator (existing suite) plus manual smoke. The existing suite does not check aria-label presence or focus ring visibility — those require either axe or eyes.
    With axe-core: Plan 02 adds test/emulator/a11y.spec.js with axe scans on popup + options in both themes; violations are reported as test failures. The package is @axe-core/playwright v4.11.3 (Deque, ~4 yrs old, no postinstall script, verified npm registry). It is a devDep — does not affect the extension bundle, store review, or runtime.
    Note: @axe-core/playwright is tagged [ASSUMED] in RESEARCH.md (slopcheck unavailable at research time). If you select "yes", Plan 02 will include a blocking checkpoint to confirm the package before install.
  </context>
  <options>
    <option id="option-yes">
      <name>Yes — add @axe-core/playwright devDep</name>
      <pros>Automated, repeatable WCAG verification on record; catches regressions in CI</pros>
      <cons>One new devDep; adds ~5 min to emulator run; [ASSUMED] package requires human legitimacy confirm before install</cons>
    </option>
    <option id="option-no">
      <name>No — manual checklist only</name>
      <pros>Zero new dependencies; fastest path to release</pros>
      <cons>A11Y verification is manual and unrepeatable; no automated regression guard</cons>
    </option>
  </options>
  <resume-signal>Select: option-yes or option-no</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML markup → accessibility tree | aria-label values are static strings in committed markup — not injectable |
| CSS token values → rendered styles | Hex color literals are hardcoded in committed CSS — not injectable |
| JS color literal → inline style | '#767676' is a hardcoded string set via style.backgroundColor — not injectable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/popup.html aria-label additions | accept | Attribute values are static string literals in committed markup; no dynamic or user-supplied content introduced |
| T-05-02 | Tampering | tokens.css hex value change | accept | Token values are hardcoded hex literals in static CSS; no new attack surface |
| T-05-03 | Tampering | create_role_list_item.js fill color | accept | backgroundColor assignment uses a hardcoded string literal '#767676'; no user input path |
| T-05-SC | Tampering | npm install (if axe-core chosen in checkpoint) | mitigate | @axe-core/playwright is [ASSUMED] — Plan 02 includes blocking-human checkpoint to verify package on npmjs.com before install |
</threat_model>

<verification>
After both auto tasks complete:
- grep -c 'aria-label="Filter roles"' src/popup.html (expect: 1)
- grep -c 'aria-label="AWS roles"' src/popup.html (expect: 1)
- grep -c 'alt="Thank you for your support."' src/popup.html (expect: 1)
- grep -c 'aria-label="AWS config (INI format)"' src/options.html (expect: 1)
- grep -c 'aria-label="Supporter key code"' src/supporters.html (expect: 1)
- grep -c '656871' src/css/tokens.css (expect: 0)
- grep -c '6e6e7a' src/css/tokens.css (expect: 2)
- grep -c 'rgb(170, 170, 170)' src/js/lib/create_role_list_item.test.js (expect: 0)
- npm test (expect: all 39 tests pass)
</verification>

<success_criteria>
- All 6 ARIA label gaps closed with attribute-only additions (no element restructuring)
- --color-border-input updated from #656871 → #6e6e7a in both dark cascade layers
- Default swatch fill updated from #aaaaaa → #767676 in JS; unit test assertions updated to rgb(118, 118, 118)
- npm test exits 0
- User has decided on axe-core devDep (yes/no) — Plan 02 executes accordingly
</success_criteria>

<output>
Create `.planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md` when done
</output>
