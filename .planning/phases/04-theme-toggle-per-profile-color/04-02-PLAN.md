---
phase: 04-theme-toggle-per-profile-color
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/js/options.js
autonomous: true
requirements:
  - THM-02
  - THM-04
must_haves:
  truths:
    - "Clicking a Visual Mode radio on the options page immediately updates data-theme on <html> and writes localStorage['visualMode'] — no page reload needed"
    - "The existing chrome.storage.sync write (syncStorageRepo.set({ visualMode })) is preserved unchanged"
    - "On options page load, if localStorage['visualMode'] differs from chrome.storage.sync['visualMode'], localStorage and data-theme are reconciled — at most one additional repaint"
    - "A storage.onChanged listener fires if another context changes visualMode in sync storage while options is open"
  artifacts:
    - path: "src/js/options.js"
      provides: "write-through radio handler + post-load reconcile + storage.onChanged listener"
      contains: "installVisualModeListener"
  key_links:
    - from: "src/js/options.js radio onchange"
      to: "localStorage + applyTheme"
      via: "localStorage.setItem + applyTheme()"
      pattern: "localStorage\\.setItem.*visualMode"
    - from: "src/js/options.js syncStorageRepo.get.then"
      to: "applyTheme"
      via: "reconcile branch"
      pattern: "applyTheme.*visualMode"
---

<objective>
Wire the theme toggle write-through into options.js. THM-02 and THM-04.

Purpose: The options page radio already writes to `chrome.storage.sync['visualMode']`. Phase 4 adds the missing two pieces: (1) immediate visual feedback via `localStorage.setItem` + `applyTheme()` on each toggle, and (2) a `storage.onChanged` listener so the page live-updates if another context changes the value while options is open.

Output: Modified `src/js/options.js` with three additions — import, radio handler augmentation, and post-load reconcile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-theme-toggle-per-profile-color/04-RESEARCH.md
@.planning/phases/04-theme-toggle-per-profile-color/04-PATTERNS.md
@.planning/phases/04-theme-toggle-per-profile-color/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import and installVisualModeListener call to options.js</name>
  <read_first>
    - src/js/options.js lines 1-20 — current import block and window.onload opening; see exact quote style (single-quote dominant, one double-quote on line 6 for profile_db import)
    - src/js/lib/theme.js — confirm the exported names applyTheme and installVisualModeListener before writing the import line
    - .planning/phases/04-theme-toggle-per-profile-color/04-PATTERNS.md lines 67-90 — exact import insertion point and installVisualModeListener placement
  </read_first>
  <files>src/js/options.js</files>
  <action>
    Make two changes to `src/js/options.js`:

    1. Add import at the end of the import block (after the existing `reloadConfig` import on line 8):
       `import { applyTheme, installVisualModeListener } from './lib/theme.js';`
       Use single quotes to match the surrounding import style.

    2. Inside `window.onload`, immediately after `const syncStorageRepo = StorageProvider.getSyncRepository();` (line 18), add a call: `installVisualModeListener();`

    No other changes in this task. 2-space indentation, consistent with the rest of options.js.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/js/options.js line 9 (or immediately after the reloadConfig import) contains: `import { applyTheme, installVisualModeListener } from './lib/theme.js';`
    - src/js/options.js contains `installVisualModeListener();` inside the `window.onload` function body, before `let configStorageArea`
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Import added; installVisualModeListener() called on options page load; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Augment radio onchange handler and post-load reconcile in options.js</name>
  <read_first>
    - src/js/options.js lines 185-220 — the existing radio onchange block (lines 191-194) and the syncStorageRepo.get callback (lines 196-219); read these before editing
    - .planning/phases/04-theme-toggle-per-profile-color/04-PATTERNS.md lines 92-119 — exact augmentation pattern for the radio handler and the reconcile site
    - .planning/phases/04-theme-toggle-per-profile-color/04-RESEARCH.md lines 117-150 — reconcile logic with the null-vs-'default' normalization note
  </read_first>
  <files>src/js/options.js</files>
  <action>
    Make two changes to the `window.onload` body in `src/js/options.js`:

    1. Augment the existing radio `onchange` handler (currently lines 191-194). After `syncStorageRepo.set({ visualMode });` (the existing write — do not remove it), add two lines:
       `localStorage.setItem('visualMode', visualMode);`
       `applyTheme(visualMode);`
       The handler uses `function` keyword — do not convert to arrow function. The `this.value` reads the radio's HTML `value` attribute, which must be `'default'`, `'light'`, or `'dark'` per the existing options.html.

    2. Inside the `syncStorageRepo.get([...]).then(data => { ... })` callback, after the existing line `elById(visualMode + 'VisualRadioButton').checked = true;` (currently line 214), add the reconcile block:
       ```
       const cachedMode = localStorage.getItem('visualMode') || 'default';
       if (cachedMode !== visualMode) {
         localStorage.setItem('visualMode', visualMode);
         applyTheme(visualMode);
       }
       ```
       The `cachedMode` normalization (`|| 'default'`) handles both `null` (absent key) and the string `'default'` as equivalent to the sync value `'default'`, preventing a spurious repaint on first install.

    The `visualMode` local variable already exists on line 213 (`const visualMode = data.visualMode || 'default'`) — do not redeclare.

    Do not add `'visualMode'` to the `syncStorageRepo.get([...])` keys array — it is already present at line 196.

    2-space indentation throughout.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/js/options.js radio onchange handler body contains both `localStorage.setItem('visualMode', visualMode);` and `applyTheme(visualMode);` after the existing `syncStorageRepo.set({ visualMode });` line
    - src/js/options.js syncStorageRepo.get callback contains `const cachedMode = localStorage.getItem('visualMode') || 'default';` followed by an `if (cachedMode !== visualMode)` block calling `localStorage.setItem` and `applyTheme`
    - `npm test` exits 0
    - The existing `syncStorageRepo.set({ visualMode })` call is unchanged
    - The `.get([...])` keys array still includes `'visualMode'` and no duplicate was added
  </acceptance_criteria>
  <done>Write-through and post-load reconcile active in options.js; unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.sync → options.js radio | Saved visualMode value loaded on page open and applied to DOM |
| options.js radio → localStorage + chrome.storage.sync | User-initiated toggle writes to both stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | applyTheme(mode) call sites in options.js | mitigate | Inherited from theme.js: only setAttribute for 'light'/'dark'; removeAttribute for all other values. The `this.value` from radio inputs can only be 'default', 'light', or 'dark' per options.html value attributes — bounded input even before applyTheme's guard |
| T-04-02 | Spoofing | post-load reconcile reads localStorage | accept | Same partition isolation as Plan 01 T-04-02; reconcile only updates localStorage and data-theme, no innerHTML or eval |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run test_emulator -- --grep "visual mode"` passes (specs from Plan 01 cover this wiring)
- options.js radio onchange handler has `localStorage.setItem('visualMode', visualMode)` and `applyTheme(visualMode)` after the existing `syncStorageRepo.set`
- options.js get callback has the `cachedMode !== visualMode` reconcile block after `elById(visualMode + 'VisualRadioButton').checked = true`
- `installVisualModeListener()` is called inside window.onload
- No change to `theme-init.js`, any CSS file, or any other JS file
</verification>

<success_criteria>
- options.js has write-through: radio toggle writes localStorage + applies data-theme immediately
- options.js has reconcile: cross-device drift fixed on page load with at most one repaint
- options.js registers storage.onChanged via installVisualModeListener
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/04-theme-toggle-per-profile-color/04-02-SUMMARY.md` when done
</output>
