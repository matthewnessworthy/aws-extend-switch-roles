---
phase: 04-theme-toggle-per-profile-color
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/js/popup.js
autonomous: true
requirements:
  - THM-04
must_haves:
  truths:
    - "On popup load, if localStorage['visualMode'] differs from chrome.storage.sync['visualMode'], data-theme is reconciled — at most one extra repaint after the pre-paint engine ran"
    - "A storage.onChanged listener fires if the options page changes visualMode while the popup is open, updating data-theme in the popup context without a reload"
    - "Per-profile color swatches (hex fill + contrast border) render without visual regression in both themes — stored hex is never mutated"
  artifacts:
    - path: "src/js/popup.js"
      provides: "post-load reconcile + storage.onChanged listener"
      contains: "installVisualModeListener"
  key_links:
    - from: "src/js/popup.js storageRepo.get"
      to: "applyTheme"
      via: "reconcile branch"
      pattern: "applyTheme.*visualMode"
    - from: "src/js/popup.js window.onload"
      to: "installVisualModeListener"
      via: "direct call"
      pattern: "installVisualModeListener"
---

<objective>
Wire the theme live-update listener and post-load reconcile into popup.js. THM-04.

Purpose: The popup is a second live extension page context. It must reconcile the pre-paint localStorage cache against chrome.storage.sync on open (one-repaint cross-device fix) and register a storage.onChanged listener so that an active options-page toggle live-repaints any already-open popup. Also verifies per-profile color SC #4 (verification only — no code change).

Output: Modified `src/js/popup.js`; manual verification of per-profile swatch rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-theme-toggle-per-profile-color/04-RESEARCH.md
@.planning/phases/04-theme-toggle-per-profile-color/04-PATTERNS.md
@.planning/phases/04-theme-toggle-per-profile-color/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import, installVisualModeListener, and post-load reconcile to popup.js</name>
  <read_first>
    - src/js/popup.js lines 1-10 — current import block and module-level `const brw = chrome || browser`; note that brw is already module-scoped here (theme.js declares its own function-scoped brw inside installVisualModeListener, so no conflict)
    - src/js/popup.js lines 100-145 — window.onload body: see the `mainEl`/`noMainEl` assignments at the top and the `storageRepo.get(['autoTabGrouping'])` call at lines 128-133
    - .planning/phases/04-theme-toggle-per-profile-color/04-PATTERNS.md lines 128-172 — exact import insertion point, installVisualModeListener placement, and storageRepo.get augmentation
    - src/js/lib/theme.js — confirm exported names before importing
  </read_first>
  <files>src/js/popup.js</files>
  <action>
    Make three changes to `src/js/popup.js`:

    1. Add import at the end of the import block (after the existing `writeProfileSetToTable` import on line 6):
       `import { applyTheme, installVisualModeListener } from './lib/theme.js';`
       Single quotes, consistent with the surrounding style.

    2. Inside `window.onload`, immediately after `noMainEl = document.getElementById('noMain');` (line 104), add: `installVisualModeListener();`

    3. Augment the existing `storageRepo.get(['autoTabGrouping'])` call (lines 128-133). Change the keys array from `['autoTabGrouping']` to `['autoTabGrouping', 'visualMode']`. Update the destructuring parameter from `({ autoTabGrouping })` to `({ autoTabGrouping, visualMode })`. Insert the reconcile block before the existing `if (autoTabGrouping)` check:
       ```
       const cachedMode = localStorage.getItem('visualMode') || 'default';
       if (cachedMode !== (visualMode || 'default')) {
         localStorage.setItem('visualMode', visualMode || 'default');
         applyTheme(visualMode || 'default');
       }
       ```
       The `|| 'default'` normalization on the sync value handles the case where `visualMode` is undefined (unset key) and correctly compares it against a cached `'default'` — no spurious repaint on clean install.

    2-space indentation throughout. Preserve the existing `if (autoTabGrouping)` block unchanged below the new reconcile code.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/js/popup.js import block contains `import { applyTheme, installVisualModeListener } from './lib/theme.js';`
    - src/js/popup.js window.onload body contains `installVisualModeListener();` before the MANY_SWITCH_COUNT constant declaration
    - src/js/popup.js storageRepo.get call uses keys `['autoTabGrouping', 'visualMode']`
    - src/js/popup.js storageRepo.get callback contains `const cachedMode = localStorage.getItem('visualMode') || 'default';` and the reconcile if-block calling `applyTheme`
    - `npm test` exits 0
    - The existing `if (autoTabGrouping)` block is present and unmodified inside the same callback
  </acceptance_criteria>
  <done>popup.js reconciles theme on open and registers the storage.onChanged listener; unit tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify per-profile color swatches render in both themes (SC #4)</name>
  <what-built>
    Option E (hex fill + 1px var(--color-border-input) contrast border) shipped in Phase 2 (popup.css .headSquare border rule) and Phase 3 (options.css #colorPicker border rule). This task confirms both render acceptably in both light and dark themes and that no stored hex value is mutated.
  </what-built>
  <how-to-verify>
    Build the extension and load it unpacked in Chrome. Have at least one role profile with a `color` key in the saved config (e.g., `color = ffcccc`).

    1. Load the popup in light theme (Visual Mode = Light). Confirm: role cards with a `color` value show a colored square with a visible border. The fill matches the stored hex; there is no color math adjusting the hue.
    2. Switch Visual Mode to Dark on the options page. Without reloading, confirm the popup live-updates its data-theme (the new installVisualModeListener wiring from Task 1).
    3. Inspect the colored role card swatches in dark theme. Confirm: the hex fill is unchanged (same color); the border provides the contrast boundary against the dark background. No swatch should be invisible or bleed into the background without any visible edge.
    4. Open the options page color picker (#colorPicker). Confirm it shows a bordered swatch in both themes.
    5. Inspect chrome.storage.sync (DevTools → Application → Storage → Extension Storage) and confirm the `color` key values are the original hex strings — not modified.

    Expected: Swatches render with visible borders in both themes. Stored hex unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if swatches render correctly in both themes, or describe any visual issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.sync → popup.js reconcile | Saved visualMode loaded on popup open and applied to DOM |
| popup.js → installVisualModeListener | storage.onChanged fires when options page writes visualMode; handled in popup page context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | applyTheme(mode) call site in popup.js reconcile | mitigate | Inherited from theme.js: guard allows only 'light'/'dark' to setAttribute; all other values removeAttribute. The `|| 'default'` normalizations in the reconcile block ensure undefined sync values map to the removeAttribute branch |
| T-04-02 | Spoofing | localStorage pre-paint cache read in popup | accept | Same extension-origin partition isolation; no cross-origin write path; validate-on-read via applyTheme's guard |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run test_emulator` full suite green
- popup.js storageRepo.get includes 'visualMode' key and reconcile block
- popup.js window.onload calls installVisualModeListener()
- Per-profile color swatches approved by human verify
- No changes to theme-init.js, any CSS file, create_role_list_item.js, or the switch action path in popup.js
</verification>

<success_criteria>
- popup.js reconciles localStorage cache on open with at most one repaint
- popup.js live-updates via storage.onChanged when options page toggles theme
- Per-profile color swatches render acceptably in both themes (human-verified)
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/04-theme-toggle-per-profile-color/04-03-SUMMARY.md` when done
</output>
