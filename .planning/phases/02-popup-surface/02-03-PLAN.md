---
phase: 02-popup-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js/popup.js
autonomous: true
requirements:
  - POP-03
  - POP-04
  - POP-05

must_haves:
  truths:
    - "popup.js no longer adds 'darkMode' class to document.body"
    - "showMessage() is replaced by four typed renderer functions: showNotOnAws, showNoRoles, showLoading, showError"
    - "showLoading() is called immediately before executeAction('loadInfo') in main()"
    - "When loadInfo resolves and profiles array is empty, showNoRoles() is called before any renderRoleList"
    - "showNoRoles() wires .aesr-open-options-link onclick at element construction time, not at window.onload"
    - "All state renderers set noMainEl.style.display = 'block' and mainEl.style.display = 'none' (inline style toggle preserved)"
    - "All renderer functions use DOM API (createElement, textContent) — no innerHTML string concatenation"
    - "OAuth Config Hub success message rendered using .aesr-state-empty__body without .aesr-alert--error class"
    - "npm test passes (33/33)"
  artifacts:
    - path: "src/js/popup.js"
      provides: "Typed state renderers, .darkMode removed, loading state added, empty-profiles guard"
      contains: "showNotOnAws"
  key_links:
    - from: "showNoRoles() — .aesr-open-options-link element"
      to: "openOptions() function"
      via: "onclick handler wired at element construction time inside showNoRoles"
      pattern: "aesr-open-options-link"
    - from: "main() — showLoading() call"
      to: "executeAction(tab.id, 'loadInfo', {})"
      via: "showLoading() called immediately before the executeAction promise"
      pattern: "showLoading"
    - from: "loadFormList — profiles guard"
      to: "showNoRoles()"
      via: "if (profiles.length === 0) { showNoRoles(); return; }"
      pattern: "profiles.length === 0"
---

<objective>
Update popup.js to: remove the .darkMode classList mutation, replace showMessage() with four typed state renderer functions (showNotOnAws, showNoRoles, showLoading, showError), add a loading state before executeAction('loadInfo'), add an empty-profiles guard in loadFormList, and route the OAuth callback showMessage calls through the new renderers.

Purpose: POP-03 (D-05/D-06 empty states), POP-04 (D-07 loading state), POP-05 (keyboard nav preserved, no CSS-class show/hide introduced).
Output: Updated popup.js with typed renderers and darkMode removal.
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-popup-surface/02-PATTERNS.md
@.planning/phases/02-popup-surface/02-RESEARCH.md
@.planning/phases/02-popup-surface/02-UI-SPEC.md
@.planning/phases/02-popup-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove .darkMode toggle and replace showMessage() with typed state renderers</name>
  <files>src/js/popup.js</files>
  <read_first>
    src/js/popup.js — full file; understand showMessage() at lines 41–47; darkMode toggle at lines 78–80; main() flow at lines 98–129 including the executeAction call at line 107, OAuth callback at lines 115–124; loadFormList location (after line 130 — read remainder of file)
    .planning/phases/02-popup-surface/02-PATTERNS.md — popup.js section: exact darkMode removal (only 3 lines deleted, rest of storageRepo.get block stays); typed renderer function implementations for showNotOnAws, showNoRoles, showLoading, showError; loading state insertion around executeAction; empty profiles guard; OAuth callback replacements
    .planning/phases/02-popup-surface/02-UI-SPEC.md — §Copywriting Contract: exact copy strings for D-05/D-06/D-07; state HTML structures
    .planning/phases/02-popup-surface/02-CONTEXT.md — D-05/D-06/D-07 locked decisions; brittle selector constraints
  </read_first>
  <action>
Read 02-PATTERNS.md before implementing — it has verbatim code for each change block below. Reading it first prevents re-deriving patterns already resolved in research.

Make these targeted changes to src/js/popup.js:

CHANGE 1 — Remove darkMode toggle (lines 78–80 only):
Delete ONLY the if-block: `if (mode === 'dark' || (mode === 'default' && window.matchMedia('(prefers-color-scheme: dark)').matches)) { document.body.classList.add('darkMode'); }`. The storageRepo.get() call at line 76, const mode assignment at line 77, and the autoTabGrouping block at lines 82–84 MUST remain — they serve other purposes.

CHANGE 2 — Replace showMessage() function (lines 41–47):
Remove the entire showMessage() function. Replace it with four typed renderer functions. All four use DOM API (createElement, textContent) — never innerHTML string concatenation. Pattern: create container element, set className, create child element(s), set textContent on each, appendChild to container, assign noMainEl.innerHTML = '' to clear previous state, appendChild container to noMainEl, set noMainEl.style.display = 'block' and mainEl.style.display = 'none'.

showNotOnAws(): Creates .aesr-state-empty div > p.aesr-state-empty__body with textContent "Navigate to the AWS console to switch roles." (D-05 exact copy). Sets show/hide state.

showNoRoles(): Creates .aesr-state-empty div > p.aesr-state-empty__body with textContent "No roles match your current account." + a.aesr-open-options-link with href="#" and textContent "Open Configuration" (D-06). Wires the link onclick handler at element construction time: link.onclick = function() { openOptions(); return false; }. Do NOT use document.querySelector to find this element later — it must be wired at creation. Sets show/hide state.

showLoading(): Creates .aesr-state-loading div > div.aesr-state-loading__spinner (empty div, CSS animates it) + p.aesr-state-loading__body with textContent "Loading roles…" (D-07 — note the ellipsis is the unicode "…" character, not three periods). Sets show/hide state.

showError(msg): Creates .aesr-alert.aesr-alert--error div > p.aesr-alert__body with body.textContent = msg (textContent, not innerHTML — msg may be server-influenced from err.message). Sets show/hide state.

In all four functions: noMainEl.style.display = 'block' and mainEl.style.display = 'none'. NEVER use classList.add('hidden') or any CSS class for show/hide — the inline style toggle is required by Playwright.

CHANGE 3 — Add loading state before executeAction('loadInfo') in main():
In the AWS URL branch inside main() (around line 107), call showLoading() immediately before executeAction(tab.id, 'loadInfo', {}). When loadInfo resolves with userInfo truthy, set mainEl.style.display = 'block' and noMainEl.style.display = 'none' before calling loadFormList. When loadInfo resolves with userInfo falsy, call showError('Failed to fetch user info from the AWS Management Console page').

CHANGE 4 — Add empty-profiles guard in loadFormList (or wherever findTargetProfiles result is consumed):
After the profiles array is obtained from findTargetProfiles(curCtx), add: if (profiles.length === 0) { showNoRoles(); return; }. This must be placed before any renderRoleList call. (Read the full popup.js file to find the exact location — it is after line 130 in the file.)

CHANGE 5 — Route OAuth callback showMessage calls through new renderers:
- Line 118 success: replace showMessage("Successfully connected to AESR Config Hub!") using the .aesr-state-empty__body approach exclusively — create a .aesr-state-empty container div containing a p.aesr-state-empty__body child, set body.textContent = "Successfully connected to AESR Config Hub!", append to noMainEl, set noMainEl.style.display = 'block'. Do NOT use .aesr-alert--error or any error-class on a success message. Do not hardcode colors. This is the locked approach for CHANGE 5 — no alternate paths.
- Line 123 error: replace showMessage(`Failed to connect to AESR Config Hub because.\n${err.message}`, 'error') with showError(`Failed to connect to AESR Config Hub because.\n${err.message}`). The showError function uses body.textContent = msg — safe for err.message which is server-influenced content.

CHANGE 6 — Remove the old non-AWS-page showMessage call (line 126):
Replace showMessage("You'll see the role list here when the current tab is AWS Management Console page.") with showNotOnAws(). The locked D-05 copy supersedes the old string.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>grep -c "classList.add('darkMode')" src/js/popup.js; echo "should be 0"</automated>
    <automated>grep -c "showNotOnAws\|showNoRoles\|showLoading\|showError" src/js/popup.js</automated>
    <automated>grep -c "aesr-open-options-link" src/js/popup.js</automated>
    <automated>grep -c "showLoading" src/js/popup.js</automated>
    <automated>grep -c "profiles.length === 0" src/js/popup.js</automated>
    <automated>grep -c "noMainEl.style.display" src/js/popup.js</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 with all 33 tests passing
    - grep for "classList.add('darkMode')" in popup.js returns 0
    - grep for "showMessage" in popup.js returns 0 (old function fully removed, all call sites replaced)
    - grep for "showNotOnAws" in popup.js returns >= 1 (function defined + called)
    - grep for "showNoRoles" in popup.js returns >= 1 (function defined + called for empty profiles)
    - grep for "showLoading" in popup.js returns >= 1 (function defined + called before executeAction loadInfo)
    - grep for "showError" in popup.js returns >= 1 (function defined + called for error states)
    - grep for "aesr-open-options-link" in popup.js returns >= 1 (CTA link class assigned in showNoRoles)
    - grep for "profiles.length === 0" in popup.js returns >= 1 (empty-profiles guard)
    - grep for "Loading roles" in popup.js returns >= 1 (D-07 copy present)
    - grep for "Navigate to the AWS console" in popup.js returns >= 1 (D-05 copy present)
    - grep for "No roles match" in popup.js returns >= 1 (D-06 copy present)
    - grep for "Open Configuration" in popup.js returns >= 1 (D-06 CTA label present)
    - grep (excluding comment lines) for "innerHTML =" in popup.js returns 0 (DOM API used throughout, no innerHTML concatenation in renderers; noMainEl.innerHTML = '' to clear is allowed — check that no string is concatenated into it)
    - The autoTabGrouping block (brw.runtime.sendMessage listenTabGroupsRemove) still exists — grep for "autoTabGrouping" in popup.js returns >= 1
    - noMainEl.style.display and mainEl.style.display toggling still present in all renderers (inline style toggle preserved — never classList)
    - OAuth Config Hub success message uses .aesr-state-empty__body (not .aesr-alert--error) — grep for "aesr-alert--error" must NOT appear in the OAuth success code path (line 118 area)
  </acceptance_criteria>
  <done>popup.js has no .darkMode class mutation, showMessage() is replaced by four typed DOM-API renderers, loading state shown before loadInfo, empty-profiles guard in loadFormList, OAuth callback routes through new renderers, and npm test passes 33/33</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| err.message → DOM | OAuth callback error (showError call at line 123) — err.message is server-influenced; must use textContent not innerHTML |
| URL pathname → renderer selection | tab.url host/pathname determines which state to show — no user-controlled string injected into DOM structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | showError(err.message) in OAuth callback | mitigate | body.textContent = msg is used throughout showError — textContent escapes HTML automatically; err.message from server is never concatenated into innerHTML |
| T-02-05 | Spoofing | showNotOnAws / showNoRoles state injection | accept | Content is authored constants (D-05/D-06 locked copy), not dynamic input; no user-controlled string reaches state container HTML |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 |
</threat_model>

<verification>
After the task completes:
- npm test — 33/33 passing
- grep for "classList.add('darkMode')" in popup.js returns 0
- grep for "showMessage" in popup.js returns 0
- grep for "showLoading" in popup.js returns >= 1
- grep for "profiles.length === 0" in popup.js returns >= 1
- Functional check: open dist/chrome/popup.html tab with a non-AWS URL active — the "Navigate to the AWS console to switch roles." message renders in the .aesr-state-empty shell
</verification>

<success_criteria>
1. popup.js has no .darkMode classList mutation; the storageRepo.get() block still exists for autoTabGrouping
2. showMessage() is fully replaced by showNotOnAws(), showNoRoles(), showLoading(), showError(msg)
3. showLoading() is called immediately before executeAction('loadInfo') in main()
4. After loadInfo resolves with empty profiles, showNoRoles() is called and renderRoleList is skipped
5. .aesr-open-options-link onclick is wired inside showNoRoles() at element construction time
6. All renderers use inline style.display toggling (never classList) for show/hide
7. All renderers use DOM API createElement/textContent (never innerHTML string concatenation)
8. npm test passes 33/33
</success_criteria>

<output>
Create .planning/phases/02-popup-surface/02-03-SUMMARY.md when done
</output>
