---
phase: 02-popup-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js/lib/create_role_list_item.js
  - src/js/lib/create_role_list_item.test.js
autonomous: true
requirements:
  - POP-02
  - POP-05
  - POP-06

must_haves:
  truths:
    - "Role list items render name on line 1 (bold) and account ID on line 2 (muted) — two-line layout (D-02)"
    - "The .aesr-role-item-text div wrapper contains .aesr-role-item__name and optionally .aesr-role-item__account spans"
    - "suffixAccountId class name no longer appears anywhere in create_role_list_item.js"
    - "All anchor dataset.* attributes and onclick handler are unchanged (Playwright + role-switch contract)"
    - "headSquare.style.backgroundColor assignment at lines 6–7 is preserved verbatim (POP-06 swatch fill)"
    - "URL validation block (lines 12–26) is preserved unchanged (security constraint)"
    - "npm test passes (all 33 unit tests including the 5 updated innerHTML assertions)"
    - "hidesAccountId: true produces the .aesr-role-item-text wrapper with only .aesr-role-item__name (no account span)"
  artifacts:
    - path: "src/js/lib/create_role_list_item.js"
      provides: "Two-line DOM — .aesr-role-item-text wrapping .aesr-role-item__name + .aesr-role-item__account"
      contains: "aesr-role-item-text"
    - path: "src/js/lib/create_role_list_item.test.js"
      provides: "Updated innerHTML assertions for two-line DOM structure"
      contains: "aesr-role-item__name"
  key_links:
    - from: "create_role_list_item.js DOM build"
      to: "popup.css .aesr-role-item-text and .aesr-role-item__name/__account rules"
      via: "class names assigned by JS consumed by CSS"
      pattern: "aesr-role-item-text"
    - from: "create_role_list_item.test.js"
      to: "create_role_list_item.js"
      via: "innerHTML exact-string assertion on the <a> element"
      pattern: "a.innerHTML.*to.eq"
---

<objective>
Update create_role_list_item.js with the two-line DOM (D-02) and update all 5 innerHTML assertions in create_role_list_item.test.js to match the new structure in the same change. These two files MUST be updated atomically — the unit tests assert exact innerHTML strings and will fail on npm test the moment the source changes without the corresponding test updates.

Purpose: POP-02 (truncation layout via .aesr-role-item-text column flex), POP-05 (test suite stays green), POP-06 (swatch fill path preserved unchanged).
Output: Updated create_role_list_item.js (two-line DOM) + updated create_role_list_item.test.js (5 innerHTML assertions).
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-popup-surface/02-PATTERNS.md
@.planning/phases/02-popup-surface/02-RESEARCH.md
@.planning/phases/02-popup-surface/02-UI-SPEC.md
@.planning/phases/02-popup-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-line DOM in create_role_list_item.js + update all 5 innerHTML assertions</name>
  <files>src/js/lib/create_role_list_item.js, src/js/lib/create_role_list_item.test.js</files>
  <read_first>
    src/js/lib/create_role_list_item.js — full file; understand the current flat append at lines 39–51 (text node + suffixAccountId span); note which lines MUST NOT change: 6–7 (backgroundColor), 12–26 (URL validation + backgroundImage), 29–37 (dataset assignments), 53–57 (onclick handler)
    src/js/lib/create_role_list_item.test.js — full file; identify the 5 innerHTML assertions at lines 41–42, 77–78, 94–95, 112–113, 169; note which tests do NOT have innerHTML assertions (region test line 134–148, malicious image test line 117–131) — those require no changes
    .planning/phases/02-popup-surface/02-PATTERNS.md — create_role_list_item.js section: exact before/after code showing the DOM restructure; create_role_list_item.test.js section: all 5 new innerHTML assertion strings
    .planning/phases/02-popup-surface/02-UI-SPEC.md — §Role List Item: the new inner DOM structure spec
  </read_first>
  <behavior>
    - Test "minimum properties" innerHTML: headSquare span + .aesr-role-item-text div containing .aesr-role-item__name span + .aesr-role-item__account span
    - Test "profile has color" innerHTML: same structure with background-color rgb(255, 170, 153) on headSquare
    - Test "profile has image" innerHTML: same structure with background-image on headSquare, no background-color
    - Test "profile has color and image" innerHTML: same structure with both background-color and background-image on headSquare
    - Test "hidesAccountId true" innerHTML: headSquare span + .aesr-role-item-text div containing ONLY .aesr-role-item__name (no .aesr-role-item__account span)
    - All dataset assertions (profile, rolename, account, displayname, color, redirecturi, search) are UNCHANGED
    - "malicious image" test (not.include assertion) requires NO change
    - "profile has region" test (dataset.redirecturi assertion) requires NO change
  </behavior>
  <action>
Update BOTH files in a single edit pass so npm test never sees a partial state.

In src/js/lib/create_role_list_item.js, replace the flat-append block (lines 39–51):

Replace:
- Line 40: anchor.appendChild(document.createTextNode(item.name))
- Lines 42–51: the if/else block that creates accountIdSpan with class="suffixAccountId" and appends it directly to anchor

With the two-line DOM (D-02 per UI-SPEC §Role List Item):
After anchor.appendChild(headSquare), create a textDiv element with class="aesr-role-item-text". Create nameSpan with class="aesr-role-item__name", set textContent to item.name, append to textDiv. In the hidesAccountId else-branch, create accountIdSpan with class="aesr-role-item__account" (not "suffixAccountId"), set textContent to item.aws_account_id, append to textDiv. Append textDiv to anchor. The dataset.displayname assignments inside the if/else stay in place — only the DOM structure changes.

PRESERVE VERBATIM (do not touch):
- Lines 3–4: headSquare creation and textContent assignment
- Lines 6–7: headSquare.style.backgroundColor conditional
- Lines 12–26: image URL validation and headSquare.style.backgroundImage
- Lines 29–37: all anchor.dataset.* assignments
- Lines 53–57: anchor.onclick handler
- Lines 64–94: createRedirectUri and createDisplayName functions

Do NOT add headSquare.style.border in JS — the border is CSS-only via popup.css .headSquare rule per D-01a.

In src/js/lib/create_role_list_item.test.js, update the 5 innerHTML assertions to match the new DOM structure. For each assertion, the new pattern wraps the text content in: <div class="aesr-role-item-text"><span class="aesr-role-item__name">...</span>[<span class="aesr-role-item__account">...</span>]</div>

Assertion details from PATTERNS.md:
- "minimum properties" (lines 41–42): headSquare with background-color rgb(170, 170, 170) + .aesr-role-item-text div with __name "profileA" + __account "222233334444"
- "profile has color" (lines 77–78): headSquare with background-color rgb(255, 170, 153) + same structure with "profile-b" and "000011115555"
- "profile has image" (lines 94–95): headSquare with background-image url (no background-color) + same structure with "prf" and "333344441111"
- "profile has color and image" (lines 112–113): headSquare with both background-color rgb(255, 170, 34) and background-image + same structure with "prf" and "333344441111"
- "hidesAccountId true" (line 169): headSquare with background-color rgb(170, 170, 170) + .aesr-role-item-text div with ONLY __name "ProfileC" — NO __account span present

The dataset assertions in all tests (a.dataset.profile, .rolename, .account, .displayname, .color, .redirecturi, .search) are unchanged — leave those lines as-is.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>grep -c "suffixAccountId" src/js/lib/create_role_list_item.js; echo "should be 0"</automated>
    <automated>grep -c "aesr-role-item-text" src/js/lib/create_role_list_item.js</automated>
    <automated>grep -c "aesr-role-item__name" src/js/lib/create_role_list_item.test.js</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 with all 33 tests passing (including the 5 updated innerHTML assertions)
    - grep for "suffixAccountId" in create_role_list_item.js returns 0
    - grep for "aesr-role-item-text" in create_role_list_item.js returns >= 1
    - grep for "aesr-role-item__account" in create_role_list_item.js returns >= 1
    - grep for "aesr-role-item__name" in create_role_list_item.test.js returns >= 1 (assertions updated)
    - grep for "suffixAccountId" in create_role_list_item.test.js returns 0 (old class name removed from assertions)
    - grep for "headSquare.style.backgroundColor" in create_role_list_item.js returns >= 1 (swatch fill preserved — D-01 POP-06)
    - Lines 12–26 of create_role_list_item.js (URL validation + backgroundImage) are unchanged — grep for "parsed.protocol" returns >= 1
    - All anchor.dataset.* assignments still present — grep for "anchor.dataset.rolename" returns >= 1
    - anchor.onclick handler still present — grep for "selectHandler" in create_role_list_item.js returns >= 1
  </acceptance_criteria>
  <done>create_role_list_item.js produces two-line DOM (D-02) with .aesr-role-item-text wrapper and .aesr-role-item__name/__account spans; create_role_list_item.test.js assertions updated to match; npm test passes 33/33</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored config → DOM text | item.name and item.aws_account_id are placed via textContent (not innerHTML) — XSS-safe |
| External image URL → CSS background-image | Validated at lines 12–26 (URL parse + protocol check) — unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | create_role_list_item.js DOM build | accept | nameSpan.textContent and accountIdSpan.textContent use textContent throughout — same safe pattern as existing code; no innerHTML concatenation introduced |
| T-02-03 | Information Disclosure | headSquare.style.backgroundImage via external URL | mitigate | URL validation block (lines 12–26) preserved verbatim; protocol whitelist (https/http only) retained |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 |
</threat_model>

<verification>
After the task completes:
- npm test — 33/33 passing with no errors in the createRoleListItem describe block
- grep -c "suffixAccountId" src/js/lib/create_role_list_item.js returns 0
- grep -c "aesr-role-item-text" src/js/lib/create_role_list_item.js returns >= 1
- grep -c "aesr-role-item__name" src/js/lib/create_role_list_item.test.js returns >= 1
</verification>

<success_criteria>
1. create_role_list_item.js produces the two-line DOM (D-02): .aesr-role-item-text div wrapping .aesr-role-item__name span (always) and .aesr-role-item__account span (when !hidesAccountId)
2. suffixAccountId class name does not appear in create_role_list_item.js
3. All lines that must be preserved are unchanged: swatch fill (lines 6–7), URL validation (lines 12–26), dataset assignments (lines 29–37), onclick handler (lines 53–57)
4. All 5 innerHTML assertions in create_role_list_item.test.js are updated to the new DOM structure
5. npm test passes 33/33
</success_criteria>

<output>
Create .planning/phases/02-popup-surface/02-02-SUMMARY.md when done
</output>
