---
phase: 02-popup-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/css/popup.css
  - src/popup.html
autonomous: true
requirements:
  - POP-01
  - POP-02
  - POP-06

must_haves:
  truths:
    - "popup.html contains no <style> block in <head>"
    - "src/css/popup.css exists and consumes only var(--token) — no hardcoded hex"
    - "popup.html links popup.css after components.css"
    - "All #roleFilter inline style= attributes are removed; the element carries class=\"aesr-input\""
    - "Role list rows and sidebar links render in Cloudscape token colors in both light and dark themes"
    - "Per-profile color swatch has a 1px border via var(--color-border-input) and var(--radius-badge) border-radius"
    - "#main and #noMain keep their display:none inline style attributes (JS/Playwright contract)"
  artifacts:
    - path: "src/css/popup.css"
      provides: "All popup-specific layout, color, scrollbar, and swatch styles"
      min_lines: 80
    - path: "src/popup.html"
      provides: "Updated HTML — no inline <style> block, popup.css linked, aesr-input class on filter, aesr-nav-group-break on Update Notice li"
      contains: "css/popup.css"
  key_links:
    - from: "src/popup.html"
      to: "src/css/popup.css"
      via: "<link rel=\"stylesheet\" href=\"css/popup.css\">"
      pattern: "href=\"css/popup.css\""
    - from: "src/css/popup.css"
      to: "src/css/tokens.css"
      via: "var(--token) references throughout"
      pattern: "var\\(--color-"
    - from: ".headSquare CSS rule"
      to: "var(--color-border-input) and var(--radius-badge)"
      via: "border and border-radius declarations"
      pattern: "color-border-input"
---

<objective>
Author src/css/popup.css (all popup-specific styles) and update src/popup.html to remove the 125-line inline <style> block, link popup.css, strip the #roleFilter inline style attribute, and add the sidebar grouping class.

Purpose: POP-01 (Cloudscape token treatment), POP-02 (truncation layout), POP-06 (swatch border, D-01/D-03).
Output: src/css/popup.css (new), src/popup.html (modified — inline <style> removed, popup.css linked, filter classed, sidebar break added).
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-popup-surface/02-UI-SPEC.md
@.planning/phases/02-popup-surface/02-PATTERNS.md
@.planning/phases/02-popup-surface/02-RESEARCH.md
@.planning/phases/02-popup-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author src/css/popup.css with all popup-specific styles</name>
  <files>src/css/popup.css</files>
  <read_first>
    src/popup.html — current structure (body, #main, #noMain, .mainPane, .optionMenu, #roleList, .headSquare, .roleListTitle, filter row)
    src/css/components.css — .aesr-input (lines 51–62), .aesr-role-item__name/__account (lines 145–153), .aesr-state-empty/loading/alert shells; do NOT redeclare font-size/color on __name/__account (already defined there)
    src/css/tokens.css — all token names to use as var(--token); --radius-xs does NOT exist; use --radius-badge (4px) for swatch
    .planning/phases/02-popup-surface/02-UI-SPEC.md — §Layout & Sizing Contract, §Component Inventory (all popup-specific shell specs), §Interaction States, §Color; token-to-hex mapping table
    .planning/phases/02-popup-surface/02-PATTERNS.md — pattern-for-popup.css section: section banner format, tab indentation, token-only discipline, what popup.css must cover (10-item list)
  </read_first>
  <action>
Create src/css/popup.css. Tab-indented throughout (matching components.css). No hardcoded hex, no hardcoded px where a spacing token exists, no @import, no !important. Follow the components.css section-banner format.

File-level header comment (adapt components.css pattern): describe popup-specific styles, aesr- prefix convention, var(--token) only.

Sections to author (in order, each with a === N. Section === banner):

1. Body / popup chrome: body with max-width 600px, max-height 600px, background-color var(--color-bg-layout), margin/padding 0, font-family var(--font-family-base). Top-level flex wrapper div: display flex (this replaces the inline style="display: flex" on popup.html line 137).

2. Main pane: .mainPane with border-right 1px solid var(--color-border-divider), padding 0 var(--space-m), min-width 280px (replaces inline style="min-width: 280px" on popup.html line 138).

3. Filter row: .aesr-popup-filter-row with display flex, align-items center, gap var(--space-xs), padding var(--space-xs) var(--space-m) var(--space-xxs). .aesr-popup-filter-row .roleListTitle: font-size var(--font-size-heading-s), font-weight var(--font-weight-bold), color var(--color-text-heading), white-space nowrap. #roleFilter.aesr-input override: flex 1, max-width none (overrides components.css width: 100% and any default max-width for the popup context). Replaces inline style="padding: 12px 6px 6px" on popup.html line 140.

4. Role list container: #roleList with padding 0, margin 0, overflow-x hidden, overflow-y auto, max-height 516px, scrollbar-width thin. #roleList li: padding 0, margin var(--space-xxs) var(--space-m) var(--space-xxs) var(--space-xs), list-style none, white-space nowrap.

5. Role list anchor (row flex — per D-02, per UI-SPEC §Role List Item): #roleList li a with display flex, flex-direction row, align-items center, gap var(--space-xs), padding var(--space-xxs) 0, text-decoration none, color var(--color-text-body), line-height normal, cursor pointer. Do NOT apply .aesr-role-item class — style the anchor directly. #roleList li a:hover: background-color var(--color-bg-dropdown-item-hover), color var(--color-text-interactive-hover).

6. Text column: .aesr-role-item-text with display flex, flex-direction column, min-width 0. Truncation on __name and __account (additive to components.css — add only overflow hidden, text-overflow ellipsis, white-space nowrap; do not redeclare font-size or color).

7. Swatch (.headSquare) per D-01/D-03: display inline-block, flex-shrink 0, width 20px, height 20px, border-radius var(--radius-badge) [NOT --radius-xs — that token does not exist], border 1px solid var(--color-border-input), background-size contain, background-repeat no-repeat, background-position center.

8. Selected row: #roleList li.selected with background-color var(--color-bg-item-selected). Exactly this selector — must match Playwright contract #roleList li.selected.

9. Sidebar nav (.optionMenu) per D-04: .optionMenu list-style none, padding/margin 0. .optionMenu li border-bottom 1px solid var(--color-border-divider-secondary). .optionMenu a: display block, font-family var(--font-family-base), font-size var(--font-size-body-m), font-weight var(--font-weight-normal), color var(--color-text-body), text-decoration none, padding var(--space-xs) var(--space-l), line-height 1.43. .optionMenu a:hover: background-color var(--color-bg-dropdown-item-hover), color var(--color-text-interactive-hover). #openOptionsLink: font-weight var(--font-weight-bold).

10. Sidebar group break (D-04a): .aesr-nav-group-break with border-top 1px solid var(--color-border-divider), margin-top var(--space-xxs), padding-top var(--space-xxs). This applies to the li wrapping "Update Notice" to create a Cloudscape-native grouping break.

11. Scrollbar (Chrome/Edge enhancement — Firefox/Edge covered by color-scheme from Phase 1): #roleList ::-webkit-scrollbar width 10px. #roleList ::-webkit-scrollbar-track background-color var(--color-bg-container). #roleList ::-webkit-scrollbar-thumb background-color var(--color-border-divider). These move from the inline <style> block's scrollbar rules updated to token values.

12. Support section: #supportComment with display none preserved as initial state via inline HTML; margin moves to CSS: margin var(--space-xs). #goldenkey margin rule: margin var(--space-xs) var(--space-s) var(--space-xs) auto.

13. Empty state CTA link: .aesr-open-options-link with color var(--color-link), text-decoration none on default state and underline on hover. This styles the "Open Configuration" link rendered by showNoRoles() in popup.js (plan 02-03 wires the handler; this plan supplies the visual rule).

Do NOT declare outline or focus-visible rules on any element — the global base.css :focus-visible rule covers #roleFilter and .optionMenu a automatically. popup.css must not override or weaken it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/css/popup.css','utf8');if(/#[0-9a-fA-F]{3,6}[^0-9a-fA-F]/.test(c)&&!/\/\*/.test(c.match(/#[0-9a-fA-F]{3,6}[^0-9a-fA-F]/)[0]))process.exit(1);console.log('ok')"</automated>
    <automated>grep -c "var(--radius-badge)" src/css/popup.css</automated>
    <automated>grep -c "radius-xs" src/css/popup.css; echo "should be 0"</automated>
    <automated>grep -c "color-border-input" src/css/popup.css</automated>
    <automated>grep -c "aesr-role-item-text" src/css/popup.css</automated>
  </verify>
  <acceptance_criteria>
    - src/css/popup.css exists with at least 80 lines
    - grep -c "var(--radius-badge)" src/css/popup.css returns >= 1 (swatch border-radius uses correct token)
    - grep for "radius-xs" in src/css/popup.css returns 0 (undefined token not used)
    - grep for "color-border-input" in src/css/popup.css returns >= 1 (.headSquare border uses this token per D-01a)
    - grep for "aesr-role-item-text" in src/css/popup.css returns >= 1 (text column wrapper styled)
    - grep for "aesr-nav-group-break" in src/css/popup.css returns >= 1 (D-04a sidebar divider)
    - No hardcoded hex color values outside of comments (no #rrggbb or #rgb in property values)
    - All CSS property values use var(--token) for color, spacing, radius, font references
    - .headSquare rule contains border-radius var(--radius-badge) and border 1px solid var(--color-border-input)
    - #roleList li a selector present with flex-direction: row (NOT .aesr-role-item class — that shell uses column)
    - #roleList li.selected selector present (exact — Playwright contract)
    - No outline property declared on any interactive element (base.css :focus-visible must not be overridden)
    - grep for "aesr-open-options-link" in src/css/popup.css returns >= 1 (CTA link color var(--color-link) styled per UI-SPEC §Color)
  </acceptance_criteria>
  <done>src/css/popup.css exists, covers all 13 sections, uses only var(--token) for color/space/radius/font, .headSquare uses var(--radius-badge) + var(--color-border-input) per D-01/D-03, and .aesr-open-options-link uses var(--color-link)</done>
</task>

<task type="auto">
  <name>Task 2: Update popup.html — remove inline style block, link popup.css, strip filter inline style, add sidebar break</name>
  <files>src/popup.html</files>
  <read_first>
    src/popup.html — full file; understand all inline style= attributes (lines 137–168) and which must be kept vs removed vs moved; note the inline <style> block spans lines 9–134
    .planning/phases/02-popup-surface/02-PATTERNS.md — popup.html delta section: keep/remove/move table for each inline style= attribute; sidebar divider approach
    .planning/phases/02-popup-surface/02-RESEARCH.md — §Source File Map → popup.html: exact disposition for each inline style attribute; sidebar D-04a note about <hr> invalidity inside <ul>
    .planning/phases/02-popup-surface/02-UI-SPEC.md — §Pattern 3 (head after Phase 2), §Sidebar Nav (D-04a resolution)
  </read_first>
  <action>
Make these changes to src/popup.html:

1. HEAD: Add <link rel="stylesheet" href="css/popup.css"> immediately after the existing <link rel="stylesheet" href="css/components.css"> line (currently line 8). The resulting order: theme-init.js → tokens.css → base.css → components.css → popup.css.

2. INLINE <style> BLOCK: Remove the entire <style>...</style> block (lines 9–134, approximately 125 lines). This removes the hardcoded hex palette, .darkMode rules, .suffixAccountId, and all scrollbar rules. After removal, popup.css becomes the sole popup stylesheet.

3. BODY inline style= attributes — apply these dispositions exactly:
   - Line 137 top-level flex <div>: remove style="display: flex" (popup.css covers this)
   - Line 138 .mainPane <div>: remove style="min-width: 280px" (popup.css covers .mainPane)
   - Line 139 #main <div>: KEEP style="display: none" — JS toggles this; Playwright contract
   - Line 140 filter-row <div>: remove style="padding: 12px 6px 6px" (popup.css .aesr-popup-filter-row covers this); also add class="aesr-popup-filter-row" to this div
   - Line 143 #roleFilter <input>: REMOVE the entire style="border:1px solid #bbb;border-radius:3px;font-size:13px;margin-left:0.4em;max-width:30ex;padding:.8ex" attribute completely; ADD class="aesr-input" to the element. The element becomes: <input id="roleFilter" class="aesr-input" type="text" placeholder="Filter">
   - Line 148 #noMain <div>: KEEP style="display: none;" — JS toggles this; Playwright contract
   - Line 163 wrapper <div> (around supportComment + goldenkey): remove style="margin: 5px" (popup.css handles support section)
   - Line 164 #supportComment <div>: KEEP style="display: none" (JS toggles); REMOVE margin: 6px from the inline style (popup.css covers margin)
   - Line 168 #goldenkey <img>: KEEP style="display: none" (JS toggles); REMOVE "margin: 6px 8px 6px auto" from the inline style (popup.css covers margin)

4. SIDEBAR GROUPING (D-04a): Add class="aesr-nav-group-break" to the <li> that wraps the "Update Notice" link (currently line 155). The element becomes: <li class="aesr-nav-group-break"><a href="#" id="openUpdateNoticeLink">Update Notice</a></li>. Do NOT use <hr> inside <ul> — it is HTML-invalid there. The .aesr-nav-group-break class on the <li> applies border-top from popup.css.
  </action>
  <verify>
    <automated>grep -c "<style>" src/popup.html; echo "should be 0"</automated>
    <automated>grep -c "css/popup.css" src/popup.html</automated>
    <automated>grep -c "aesr-input" src/popup.html</automated>
    <automated>grep -c "aesr-nav-group-break" src/popup.html</automated>
    <automated>grep -c 'id="main".*display: none\|display: none.*id="main"' src/popup.html; echo "checking #main keeps display:none"</automated>
    <automated>grep -c 'id="noMain"' src/popup.html</automated>
  </verify>
  <acceptance_criteria>
    - grep for "<style>" in src/popup.html returns 0 (inline style block fully removed)
    - grep for "css/popup.css" in src/popup.html returns 1 (link element present)
    - grep for "aesr-input" in src/popup.html returns >= 1 (#roleFilter has class)
    - grep for 'style="border:' in src/popup.html returns 0 (all inline border style on #roleFilter removed)
    - grep for "aesr-nav-group-break" in src/popup.html returns 1 (Update Notice li has class)
    - src/popup.html still contains id="main" with style="display: none" (Playwright contract intact)
    - src/popup.html still contains id="noMain" with style="display: none" (Playwright contract intact)
    - src/popup.html still contains id="roleFilter" (ID unchanged — Playwright contract)
    - The <link> order in <head> is: theme-init.js → tokens.css → base.css → components.css → popup.css (no other sheets follow)
    - grep for "darkMode" in src/popup.html returns 0 (removed with style block)
    - grep for "suffixAccountId" in src/popup.html returns 0 (removed with style block)
  </acceptance_criteria>
  <done>popup.html has no inline <style> block, links popup.css in correct cascade position, #roleFilter has class="aesr-input" with no inline style= attribute, Update Notice li has class="aesr-nav-group-break", and all display:none toggleable elements retain their inline style</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored profile hex → CSS fill | headSquare.style.backgroundColor set from stored profile color — unchanged by Phase 2; CSS-only border added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | headSquare fill via stored hex | accept | Stored color is user-configured; no new input path; V5 URL validation at create_role_list_item.js:12–26 is unchanged by this plan |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2; zero new runtime or dev dependencies |
</threat_model>

<verification>
After both tasks complete:
- npm test — 33/33 passing (no regressions; popup.css change does not affect unit tests)
- Visual: open dist/chrome/popup.html in a browser, toggle data-theme on <html> between light and dark — role list, filter, swatch, sidebar, and state containers render correctly in both themes
- Confirm no <style> block in popup.html head: grep -c "<style>" src/popup.html returns 0
- Confirm popup.css has no hardcoded hex: grep -E "#[0-9a-fA-F]{3,6}" src/css/popup.css should return only comment lines
- Wave gate (run after all wave 1 plans complete): npm run test_emulator -- test/emulator/keyboard_navigation.spec.js test/emulator/keyboard_edge_cases.spec.js — guards brittle selectors (#roleFilter, #roleList li.selected, li[style*="block"]) that POP-05 requires unchanged
</verification>

<success_criteria>
1. src/css/popup.css exists, covers all popup-specific layout and color using var(--token) only, and .headSquare uses var(--radius-badge) and var(--color-border-input) per D-01/D-03
2. popup.html has no inline <style> block, links popup.css after components.css, #roleFilter has class="aesr-input" with no inline style=, Update Notice li has class="aesr-nav-group-break"
3. All display:none inline style= attributes on JS-toggled elements (#main, #noMain, #supportComment, #goldenkey) are preserved unchanged
4. npm test passes (33/33)
</success_criteria>

<output>
Create .planning/phases/02-popup-surface/02-01-SUMMARY.md when done
</output>
