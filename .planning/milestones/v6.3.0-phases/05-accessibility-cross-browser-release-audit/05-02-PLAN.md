---
phase: 05-accessibility-cross-browser-release-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - test/emulator/a11y.spec.js
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - A11Y-01
  - A11Y-04
  - A11Y-05
user_setup: []

must_haves:
  truths:
    - "The full unit + emulator suite is green after the Plan 01 code changes land"
    - "A11Y verification coverage is on record (axe spec if option-yes, acknowledged manual-only if option-no)"
    - "Firefox and Edge have been manually smoked in both themes"
  artifacts:
    - path: "test/emulator/a11y.spec.js"
      provides: "Axe-core WCAG scans on popup + options in both themes (only if option-yes was chosen in Plan 01)"
  key_links:
    - from: "test/emulator/a11y.spec.js"
      to: "fixtures.js testInPopup / testInOptions"
      via: "import { testInPopup, testInOptions } from './fixtures.js'"
      pattern: "testInPopup|testInOptions"
---

<objective>
Run the full test suite and execute a11y verification — either automated via axe-core spec (if option-yes was chosen) or confirmed via manual smoke. Then execute the cross-browser manual smoke for Firefox and Edge (A11Y-05).

Purpose: Validates that all Plan 01 code changes landed cleanly (no broken selectors, no test regressions) and that A11Y-01 (focus visibility) and A11Y-02 (label coverage) are confirmed via automated scan or acknowledged manual check.

Output: Green npm test + npm run test_emulator; test/emulator/a11y.spec.js (if axe path chosen); manual smoke confirmed for Chrome, Firefox, Edge in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-RESEARCH.md
@.planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm Plan 01 changes landed cleanly (A11Y-04)</name>
  <files></files>
  <read_first>
    .planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md — check the option-yes/option-no decision from the Plan 01 checkpoint; note it for Tasks 2 and 3
  </read_first>
  <action>
    Run: npm test
    If any unit test fails, debug before proceeding — Plan 01 changes (create_role_list_item.js fill + test assertions) must be in place.

    Run: npm run test_emulator
    This runs the 7 Playwright emulator spec files against the Chrome extension. The aria-label additions in popup.html, options.html, and supporters.html must not break any existing ID-selector locators (#roleFilter, #roleList, #awsConfigTextArea, #colorPicker, #colorValue, #textareaKeyCode, #keyCodeValid, #keyCodeInvalid).

    Both commands must exit 0 before proceeding to Task 2.

    Note in the summary: which decision (option-yes or option-no) was recorded in the Plan 01 checkpoint. Tasks 2 and 3 are conditional on that decision.
  </action>
  <verify>
    <automated>npm test && npm run test_emulator</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (all unit tests pass — no regression from Plan 01 JS/test changes)
    - npm run test_emulator exits 0 (all 7 emulator spec files pass — aria-label additions did not break any ID-selector Playwright locator)
  </acceptance_criteria>
  <done>Full automated test suite green after Plan 01 changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    @axe-core/playwright v4.11.3 (Deque) is tagged [ASSUMED] in the research — slopcheck was unavailable when the research was written. This checkpoint confirms package legitimacy before the npm install in Task 3.

    If option-no was selected in Plan 01: SKIP this checkpoint entirely and proceed to Task 4.
  </what-built>
  <how-to-verify>
    Visit: https://www.npmjs.com/package/@axe-core/playwright

    Confirm ALL of the following before approving:
    1. Version 4.11.3 is published (or a newer patch version exists)
    2. Publisher is "dequelabs" or "Deque Systems"
    3. Source repository is github.com/dequelabs/axe-core-npm
    4. Package has no "postinstall" script listed under Scripts
    5. Weekly downloads are > 500,000 (established package, not a newly published impersonator)

    If any check fails: type "reject — [reason]" and do not proceed to Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming all 5 checks, or "skip" if option-no was chosen, or "reject — [reason]" if any check fails</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install axe-core and write a11y spec (A11Y-01, A11Y-02) — option-yes only</name>
  <files>test/emulator/a11y.spec.js, package.json, package-lock.json</files>
  <read_first>
    .planning/phases/05-accessibility-cross-browser-release-audit/05-01-SUMMARY.md — confirm option-yes was chosen; if option-no, skip this task entirely (no files to create)
    test/emulator/fixtures.js — read in full; confirm testInPopup and testInOptions signatures (before, pageFunc, after args)
    test/emulator/visual_mode.spec.js — reference for testInOptions dual-theme pattern (lines 40-55: set visualMode via storage, waitForTimeout(300), check data-theme attribute)
    .planning/phases/05-accessibility-cross-browser-release-audit/05-PATTERNS.md — section "test/emulator/a11y.spec.js" for the axe call sequence and fixture usage
  </read_first>
  <action>
    SKIP IF option-no: if option-no was chosen in the Plan 01 checkpoint, do nothing in this task and proceed to Task 4.

    ONLY if option-yes and legitimacy checkpoint (Task 2) returned "approved":

    1. Install: npm install --save-dev @axe-core/playwright
       This updates package.json devDependencies and package-lock.json.

    2. Create test/emulator/a11y.spec.js with this structure:
       - Import: import AxeBuilder from '@axe-core/playwright'
       - Import: import { testInPopup, testInOptions } from './fixtures.js'
       - Test 1: "a11y: popup passes WCAG 2.1 AA in light theme"
         testInPopup with before=chrome.storage.sync.clear, pageFunc runs AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze() then asserts accessibilityScanResults.violations.length === 0
       - Test 2: "a11y: popup passes WCAG 2.1 AA in dark theme"
         testInPopup with before=chrome.storage.sync.set({visualMode:'dark'}), pageFunc sets dark theme via page.evaluate(()=>chrome.storage.sync.set({visualMode:'dark'})) + page.waitForTimeout(300), then confirms data-theme==='dark' via page.evaluate, then runs axe scan; after=chrome.storage.sync.clear
       - Test 3: "a11y: options page passes WCAG 2.1 AA in light theme"
         testInOptions with before=chrome.storage.sync.clear, pageFunc runs axe scan, after=chrome.storage.sync.clear
       - Test 4: "a11y: options page passes WCAG 2.1 AA in dark theme"
         testInOptions with before=chrome.storage.sync.set({visualMode:'dark'}), pageFunc sets dark theme via page.evaluate + waitForTimeout(300) + data-theme check + axe scan; after=chrome.storage.sync.clear

    3. Run: npm run test_emulator
       All 4 new axe tests must pass. If violations are reported, the Plan 01 ARIA/contrast fixes landed correctly and no new violations exist.
  </action>
  <verify>
    <automated>npm run test_emulator</automated>
  </verify>
  <acceptance_criteria>
    - If option-no: no files created; task is vacuous pass
    - If option-yes: test/emulator/a11y.spec.js exists and contains 4 test cases (popup light, popup dark, options light, options dark)
    - If option-yes: a11y.spec.js imports AxeBuilder from '@axe-core/playwright' and uses .withTags(['wcag2a', 'wcag2aa'])
    - If option-yes: package.json devDependencies contains @axe-core/playwright
    - If option-yes: npm run test_emulator exits 0 with all tests passing (0 axe violations)
  </acceptance_criteria>
  <done>Axe spec created and passing (option-yes) or skipped cleanly (option-no)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All code changes (Plan 01) and all automated verification (Plan 02 Tasks 1-3) are complete. The full test suite is green.
  </what-built>
  <how-to-verify>
    Manually smoke test the extension in Firefox and Edge in both themes (A11Y-05). The Playwright emulator runs Chrome only — Firefox and Edge require your installed browsers.

    Steps to load the extension:
    1. Run: npm run build (if not already done — ensures dist/ is fresh for this smoke)
    2. Firefox: about:debugging → "This Firefox" → Load Temporary Add-on → select dist/firefox/manifest.json
    3. Edge: edge://extensions → Developer mode → Load unpacked → select dist/chrome/

    Smoke checklist (repeat for each browser, BOTH light and dark themes):

    POPUP (click extension icon):
    - [ ] Role list renders — items visible with correct font and spacing
    - [ ] Color swatches render — default gray swatch visible against item background (new gray is slightly darker)
    - [ ] Filter input: click to focus → blue outline ring visible
    - [ ] Arrow-key navigation: Tab into list → arrow keys move focus between role items
    - [ ] Theme switch: set Light via Options, verify popup reflects light; set Dark, verify popup reflects dark

    OPTIONS PAGE (open Options):
    - [ ] Page layout renders — containers, cards, header correct in both themes
    - [ ] INI textarea: click to focus → blue outline ring visible
    - [ ] Color picker pair: tab through #colorPicker and #colorValue → both show focus rings
    - [ ] Visual mode radios: selecting each changes page theme immediately
    - [ ] Form controls (radios, checkboxes) styled correctly in both themes

    NATIVE CONTROLS (critical for Firefox/Edge cross-browser parity):
    - [ ] Scrollbars follow theme (dark scrollbars in dark mode)
    - [ ] Checkboxes render in theme-appropriate style
    - [ ] Textarea border is visible in dark theme (uses updated #6e6e7a)

    SUPPORTERS PAGE (if accessible):
    - [ ] Page renders; key code textarea focusable with visible ring
  </how-to-verify>
  <resume-signal>Type "smoke-passed" if all browsers pass both themes, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install (if axe-core) → devDependency | Package install touches only node_modules and lockfile; does not affect extension bundle |
| Playwright test → chrome-extension:// URL | Extension runs in isolated browser context; axe injects via page.evaluate — same trust boundary as existing emulator tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | @axe-core/playwright npm install | mitigate | Package is [ASSUMED] — Task 2 is a blocking-human checkpoint: verify v4.11.3 on npmjs.com/package/@axe-core/playwright, confirm Deque authorship, no postinstall script, correct source repo, >500K weekly downloads before install |
| T-05-02-02 | Information Disclosure | axe scan output in test logs | accept | axe reports HTML structure and ARIA attributes — no PII, no secrets; test output is local dev only |
| T-05-SC | Tampering | npm installs generally | mitigate | Legitimacy checkpoint is blocking-human (Task 2); auto_advance does not bypass it; applies only if option-yes |
</threat_model>

<verification>
After Task 1:
- npm test exits 0
- npm run test_emulator exits 0

After Task 3 (option-yes only):
- npm run test_emulator exits 0 including all 4 new axe tests (0 violations)

After manual smoke checkpoint (Task 4):
- Firefox popup + options: both themes render correctly
- Edge popup + options: both themes render correctly
- Native controls follow theme in both browsers
</verification>

<success_criteria>
- npm test + npm run test_emulator green (zero regressions from Plan 01 changes)
- A11Y coverage documented — axe spec (option-yes) or acknowledged manual-only (option-no)
- Firefox and Edge manually smoked in both themes; smoke-passed confirmed
</success_criteria>

<output>
Create `.planning/phases/05-accessibility-cross-browser-release-audit/05-02-SUMMARY.md` when done
</output>
